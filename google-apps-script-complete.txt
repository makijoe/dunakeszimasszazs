// ============================================
// Google Apps Script for Dunakeszi Masszázs
// Complete Booking & Customer Management System
// With P&L Tracking and Session Package Management
// ============================================

// Sheet names
const SHEETS = {
  CUSTOMERS: 'Customers',
  SESSION_PACKAGES: 'SessionPackages',
  BOOKINGS: 'Bookings',
  FINANCIAL: 'Financial',
  PNL: 'PnL',
  LOGS: 'ErrorLogs'
};

// ============================================
// Setup - Run this once to create sheets
// ============================================
function setupSheets() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  
  // Create Customers sheet
  let customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  if (!customersSheet) {
    customersSheet = ss.insertSheet(SHEETS.CUSTOMERS);
    customersSheet.appendRow([
      'CustomerID', 'Name', 'Email', 'Phone', 'CreatedDate', 
      'TotalSessionsPurchased', 'TotalSessionsUsed', 'TotalSessionsRemaining',
      'TotalSpent', 'Notes'
    ]);
    customersSheet.getRange(1, 1, 1, 10).setFontWeight('bold');
  }
  
  // Create Session Packages sheet
  let packagesSheet = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
  if (!packagesSheet) {
    packagesSheet = ss.insertSheet(SHEETS.SESSION_PACKAGES);
    packagesSheet.appendRow([
      'PackageID', 'CustomerID', 'CustomerName', 'PurchaseDate', 
      'ServiceType', 'SessionsPurchased', 'SessionsUsed', 'SessionsRemaining',
      'OriginalPrice', 'DiscountPercent', 'DiscountAmount', 'FinalPrice',
      'DepositPaid', 'Status', 'Notes'
    ]);
    packagesSheet.getRange(1, 1, 1, 15).setFontWeight('bold');
  }
  
  // Create Bookings sheet
  let bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  if (!bookingsSheet) {
    bookingsSheet = ss.insertSheet(SHEETS.BOOKINGS);
    bookingsSheet.appendRow([
      'BookingID', 'CustomerID', 'CustomerName', 'Service', 
      'Date', 'Time', 'Status', 'PackageID', 'SessionNumber',
      'CreatedDate', 'Notes'
    ]);
    bookingsSheet.getRange(1, 1, 1, 11).setFontWeight('bold');
  }
  
  // Create Financial Transactions sheet
  let financialSheet = ss.getSheetByName(SHEETS.FINANCIAL);
  if (!financialSheet) {
    financialSheet = ss.insertSheet(SHEETS.FINANCIAL);
    financialSheet.appendRow([
      'TransactionID', 'Date', 'Type', 'CustomerID', 'CustomerName',
      'Description', 'Amount', 'PaymentMethod', 'RelatedPackageID', 'Notes'
    ]);
    financialSheet.getRange(1, 1, 1, 10).setFontWeight('bold');
  }
  
  // Create P&L Summary sheet
  let pnlSheet = ss.getSheetByName(SHEETS.PNL);
  if (!pnlSheet) {
    pnlSheet = ss.insertSheet(SHEETS.PNL);
    pnlSheet.appendRow(['Dunakeszi Masszázs - P&L Dashboard']);
    pnlSheet.appendRow(['']);
    pnlSheet.appendRow(['Monthly Summary']);
    pnlSheet.appendRow(['Month', 'Total Income', 'Total Deposits', 'Outstanding', 'Sessions Completed']);
    pnlSheet.getRange(3, 1, 2, 5).setFontWeight('bold');
  }
  
  return 'Sheets created successfully!';
}

// ============================================
// Main POST Handler
// ============================================
function doPost(e) {
  var lock = LockService.getScriptLock();
  try {
    lock.waitLock(30000);
    var data = {};
    
    // Google Apps Script form data is in e.parameter
    if (e && e.parameter && e.parameter.action) {
      data = e.parameter;
    } else if (e && e.postData && e.postData.contents) {
      // Try to parse as JSON
      try {
        data = JSON.parse(e.postData.contents);
      } catch (jsonError) {
        // If not JSON, parse as form data string
        var formData = e.postData.contents;
        var pairs = formData.split('&');
        for (var i = 0; i < pairs.length; i++) {
          var pair = pairs[i].split('=');
          if (pair.length === 2) {
            data[decodeURIComponent(pair[0])] = decodeURIComponent(pair[1].replace(/\+/g, ' '));
          }
        }
      }
    }
    
    // Stripe sends webhook events as JSON with a 'type' field but no 'action' field
    if (!data.action && data.type) {
      return handleStripeWebhook(data);
    }

    if (!data.action) {
      return createHtmlResponse('<h1>Hiba</h1><p>Hiányzó művelet</p>');
    }
    
    switch(data.action) {
      case 'ping':
        return createResponse(true, 'pong');
      case 'createBooking':
        return createBooking(data);
      case 'purchasePackage':
        return purchasePackage(data);
      case 'useSession':
        return useSession(data);
      case 'getCustomerProfile':
        return getCustomerProfile(data);
      case 'getAllCustomers':
        return getAllCustomers();
      case 'getPnL':
        return getPnL(data);
      case 'cancel':
        return handleCancellation(data);
      case 'change':
        return handleChange(data);
      case 'selfCancel':
        return selfCancelBooking_(data);
      case 'selfChange':
        return selfRequestChange_(data);
      case 'blockSlot':
        return blockSlot_(data);
      case 'createStripeCheckout':
        return createStripeCheckout(data);
      case 'createStripeCheckoutDebug':
        return createStripeCheckoutDebug(data);
      case 'createBankTransferBooking':
        return createBankTransferBooking(data);
      case 'confirmBankTransfer':
        return confirmBankTransferBooking(data);
      default:
        return createHtmlResponse('<h1>Hiba</h1><p>Ismeretlen művelet: ' + data.action + '</p>');
    }
  } catch (error) {
    logError('doPost', error);
    return createHtmlResponse('<h1>Hiba</h1><p>' + error.message + '</p>');
  } finally {
    lock.releaseLock();
  }
}

// ============================================
// GET Handler for Dashboard Data
// ============================================
function doGet(e) {
  try {
    var action = e.parameter.action;
    
    switch(action) {
      case 'dashboard':
        return getDashboardData();
      case 'customer':
        return getCustomerProfile({ email: e.parameter.email });
      case 'pnl':
        return getPnL({ mode: e.parameter.mode, month: e.parameter.month, year: e.parameter.year, date: e.parameter.date, weekStart: e.parameter.weekStart });
      case 'allBookings':
        return getAllBookings_();
      case 'pendingBookings':
        return getPendingBookings_();
      case 'allPackages':
        return getAllPackages_();
      case 'checkSlot':
        return createResponse(isTimeSlotAvailable(e.parameter.date, e.parameter.time), isTimeSlotAvailable(e.parameter.date, e.parameter.time) ? 'Szabad' : 'Foglalt');
      case 'getSlotsForDate':
        return getSlotsForDate_(e.parameter.date);
      case 'myBookings':
        return getMyBookings_(e.parameter.email);
      default:
        return createResponse(true, 'API is running', { 
          endpoints: ['dashboard', 'customer', 'pnl'] 
        });
    }
  } catch (error) {
    return createResponse(false, 'Hiba: ' + error.message);
  }
}

// ============================================
// Create New Booking with Session Tracking
// ============================================
function createBooking(data) {
  // Validate required fields
  if (!data.name || !data.email || !data.service || !data.date || !data.time) {
    return createResponse(false, 'Hiányzó kötelező mezők');
  }
  
  var customer = getOrCreateCustomer(data.name, data.email, data.phone);
  var customerId = customer.customerId;

  // Prevent same customer from double-booking the same date + time
  // 1) Check in-memory cache first (GAS read cache means sheet reads are stale in same execution)
  if (_createdBookingKeys[customerId + '|' + data.date + '|' + data.time]) {
    return createResponse(false, 'Ezen az időponton már van aktív foglalásod! Kérjük válassz másik időpontot.');
  }
  // 2) Check persisted sheet data (catches cross-execution duplicates)
  var dupSheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEETS.BOOKINGS);
  var dupData = dupSheet.getDataRange().getValues();
  for (var di = 1; di < dupData.length; di++) {
    if (dupData[di][1] === customerId && (dupData[di][6] === 'Confirmed' || dupData[di][6] === 'ChangeRequested')) {
      var dupDate = typeof dupData[di][4] === 'string' ? dupData[di][4].slice(0,10) : formatDate(new Date(dupData[di][4]));
      var dupTime = formatTimeFromSheet(dupData[di][5]);
      if (dupDate === data.date && dupTime === data.time) {
        return createResponse(false, 'Ezen az időponton már van aktív foglalásod! Kérjük válassz másik időpontot.');
      }
    }
  }

  // Check if customer has active session package
  var activePackage = findActivePackage(customerId, data.service);

  var packageId = null;
  var sessionNumber = null;

  // Handle recurring booking → auto-create a bérlet (SessionPackages entry)
  var recurringCountEarly = (data.recurring === true || data.recurring === 'yes' || data.recurring === 'true') ? (parseInt(data.recurringCount) || 1) : 1;

  if (recurringCountEarly >= 2) {
    // Auto-create a bérlet for this multi-session booking
    var ss2 = SpreadsheetApp.getActiveSpreadsheet();
    var packagesSheet2 = ss2.getSheetByName(SHEETS.SESSION_PACKAGES);
    var autoPackageId = 'PKG' + Date.now();
    var pricePerSession = 15000; // all services are 15 000 Ft
    var autoOriginalPrice = recurringCountEarly * pricePerSession;
    var autoDiscountPct = calculateDiscount(recurringCountEarly);
    var autoDiscountAmt = Math.round(autoOriginalPrice * (autoDiscountPct / 100));
    var autoFinalPrice = autoOriginalPrice - autoDiscountAmt;
    // deposit already paid by customer via Stripe (passed in as data.amount in Ft)
    var autoDepositPaid = parseInt(data.amount) || Math.round(autoFinalPrice * 0.2);
    packagesSheet2.appendRow([
      autoPackageId,
      customerId,
      data.name,
      formatDate(new Date()),
      data.service || 'Bármely kezelés',
      recurringCountEarly,   // sessionsPurchased
      0,                     // sessionsUsed (none completed yet)
      recurringCountEarly,   // sessionsRemaining
      autoOriginalPrice,
      autoDiscountPct,
      autoDiscountAmt,
      autoFinalPrice,
      autoDepositPaid,
      'Active',
      'Automatikusan létrehozva – ismétlődő foglalásból (' + recurringCountEarly + ' alkalom)'
    ]);
    SpreadsheetApp.flush();
    updateCustomerSessionTotals(customerId);
    packageId = autoPackageId;
  } else if ((data.usePackage === true || data.usePackage === 'true') && activePackage) {
    // Single-session booking deducting from an existing package
    var useResult = deductSessionFromPackage(activePackage.packageId);
    if (useResult.success) {
      packageId = activePackage.packageId;
      sessionNumber = useResult.sessionNumber;
    }
  }

  // Create calendar event
  var calendar = CalendarApp.getDefaultCalendar();
  var dateParts = data.date.split('-');
  var timeParts = data.time.split(':');
  var startTime = new Date(
    parseInt(dateParts[0]), parseInt(dateParts[1]) - 1, parseInt(dateParts[2]),
    parseInt(timeParts[0]), parseInt(timeParts[1])
  );
  var endTime = new Date(startTime.getTime() + (75 * 60 * 1000));
  
  // Handle recurring bookings
  var recurringCount = (data.recurring === true || data.recurring === 'yes' || data.recurring === 'true') ? (parseInt(data.recurringCount) || 1) : 1;
  var recurringType = data.recurringType || 'weekly';
  var createdEvents = [];
  var createdBookingIds = [];
  var recurringDates = [];
  
  var discountPercent = calculateDiscount(recurringCount);
  
  for (var i = 0; i < recurringCount; i++) {
    var occurrenceStart = new Date(startTime);
    var occurrenceEnd = new Date(endTime);
    
    if (i > 0) {
      if (recurringType === 'weekly') {
        occurrenceStart.setDate(occurrenceStart.getDate() + (i * 7));
        occurrenceEnd.setDate(occurrenceEnd.getDate() + (i * 7));
      } else if (recurringType === 'biweekly') {
        occurrenceStart.setDate(occurrenceStart.getDate() + (i * 14));
        occurrenceEnd.setDate(occurrenceEnd.getDate() + (i * 14));
      } else if (recurringType === 'monthly') {
        occurrenceStart.setMonth(occurrenceStart.getMonth() + i);
        occurrenceEnd.setMonth(occurrenceEnd.getMonth() + i);
      }
    }
    
    var dateStr = formatDate(occurrenceStart);
    recurringDates.push(dateStr);
    
    // Create calendar event
    var eventTitle = 'Masszázs - ' + data.name + ' - ' + data.service;
    if (recurringCount > 1) eventTitle += ' (' + (i + 1) + '/' + recurringCount + ')';
    if (packageId) eventTitle += ' [Bérlet]';
    
    var event = calendar.createEvent(eventTitle, occurrenceStart, occurrenceEnd, {
      description: buildEventDescription(data, i + 1, recurringCount, packageId, sessionNumber),
      location: 'Angyali Szalon, Dunakeszi'
    });
    event.setColor(packageId ? '2' : '6'); // Green for package sessions, orange for regular
    var eventId = event.getId();
    createdEvents.push(eventId);
    
    // Record booking (including calendar event ID and Stripe payment intent for refunds)
    var newBookingId = recordBooking(customerId, data.name, data.service, dateStr, data.time, packageId, sessionNumber, eventId, data.stripePaymentIntentId || '');
    createdBookingIds.push(newBookingId);
    // Register in-memory so same-execution duplicate checks catch this immediately
    _createdBookingKeys[customerId + '|' + dateStr + '|' + data.time] = true;
  }
  
  // Send confirmation emails
  sendBookingConfirmationToEdina(data, recurringCount, recurringType, recurringDates, discountPercent, packageId);
  sendBookingConfirmationToCustomer(data, recurringCount, recurringType, recurringDates, discountPercent, packageId, customer);
  
  return createResponse(true, 'Foglalás sikeresen rögzítve!', {
    customerId: customerId,
    eventIds: createdEvents,
    bookingIds: createdBookingIds,
    recurringCount: recurringCount,
    recurringDates: recurringDates,
    discountPercent: discountPercent,
    packageUsed: packageId !== null,
    customerProfile: customer
  });
}

// ============================================
// Purchase Session Package
// ============================================
function purchasePackage(data) {
  if (!data.customerId && !data.email) {
    return createResponse(false, 'CustomerID vagy email szükséges');
  }
  
  var customer;
  if (data.customerId) {
    customer = getCustomerById(data.customerId);
  } else {
    customer = getOrCreateCustomer(data.name, data.email, data.phone);
  }
  
  if (!customer) {
    return createResponse(false, 'Vásárló nem található');
  }
  
  var packageId = 'PKG' + Date.now();
  var purchaseDate = new Date();
  
  var sessionsPurchased = data.sessions || 12;
  var originalPrice = data.originalPrice || (sessionsPurchased * 15000);
  var discountPercent = calculateDiscount(sessionsPurchased);
  var discountAmount = Math.round(originalPrice * (discountPercent / 100));
  var finalPrice = originalPrice - discountAmount;
  var depositPaid = data.depositPaid || Math.round(finalPrice * 0.2);
  
  // Record package
  var packagesSheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEETS.SESSION_PACKAGES);
  packagesSheet.appendRow([
    packageId,
    customer.customerId,
    customer.name,
    formatDate(purchaseDate),
    data.service || 'Bármely kezelés',
    sessionsPurchased,
    0, // sessions used
    sessionsPurchased, // sessions remaining
    originalPrice,
    discountPercent,
    discountAmount,
    finalPrice,
    depositPaid,
    'Active',
    data.notes || ''
  ]);
  
  // Record financial transaction
  recordTransaction(purchaseDate, 'Deposit', customer.customerId, customer.name, 
    'Bérlet foglaló (' + sessionsPurchased + ' alkalom)', depositPaid, 'Bank Transfer', packageId);
  
  // Update customer totals
  updateCustomerSessionTotals(customer.customerId);
  
  // Send confirmation email
  sendPackagePurchaseConfirmation(customer, {
    packageId: packageId,
    sessions: sessionsPurchased,
    originalPrice: originalPrice,
    discountPercent: discountPercent,
    discountAmount: discountAmount,
    finalPrice: finalPrice,
    depositPaid: depositPaid
  });
  
  return createResponse(true, 'Bérlet sikeresen létrehozva!', {
    packageId: packageId,
    customerId: customer.customerId,
    sessionsPurchased: sessionsPurchased,
    finalPrice: finalPrice,
    depositPaid: depositPaid
  });
}

// ============================================
// Use/Deduct Session from Package
// ============================================
function useSession(data) {
  if (!data.packageId && !data.customerId) {
    return createResponse(false, 'PackageID vagy CustomerID szükséges');
  }
  
  var pkg;
  if (data.packageId) {
    pkg = getPackageById(data.packageId);
  } else {
    pkg = findActivePackage(data.customerId, data.service);
  }
  
  if (!pkg || pkg.sessionsRemaining <= 0) {
    return createResponse(false, 'Nincs aktív bérlet vagy elfogytak az alkalmak');
  }
  
  return deductSessionFromPackage(pkg.packageId);
}

function deductSessionFromPackage(packageId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var packagesSheet = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
  var data = packagesSheet.getDataRange().getValues();
  
  for (var i = 1; i < data.length; i++) {
    if (data[i][0] === packageId) {
      var sessionsUsed = data[i][6] + 1;
      var sessionsRemaining = data[i][5] - sessionsUsed;
      
      packagesSheet.getRange(i + 1, 7).setValue(sessionsUsed);
      packagesSheet.getRange(i + 1, 8).setValue(sessionsRemaining);
      
      if (sessionsRemaining <= 0) {
        packagesSheet.getRange(i + 1, 14).setValue('Completed');
      }
      
      // Update customer totals
      updateCustomerSessionTotals(data[i][1]);
      
      return {
        success: true,
        packageId: packageId,
        sessionNumber: sessionsUsed,
        sessionsRemaining: sessionsRemaining
      };
    }
  }
  
  return { success: false, message: 'Bérlet nem található' };
}

// ============================================
// Customer Management
// ============================================
function getOrCreateCustomer(name, email, phone) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var data = customersSheet.getDataRange().getValues();
  
  // Search by email
  for (var i = 1; i < data.length; i++) {
    if (data[i][2] === email) {
      return {
        customerId: data[i][0],
        name: data[i][1],
        email: data[i][2],
        phone: data[i][3],
        createdDate: data[i][4],
        totalSessionsPurchased: data[i][5],
        totalSessionsUsed: data[i][6],
        totalSessionsRemaining: data[i][7],
        totalSpent: data[i][8],
        notes: data[i][9],
        isNew: false
      };
    }
  }
  
  // Create new customer
  var customerId = 'CUST' + Date.now();
  var createdDate = new Date();
  
  customersSheet.appendRow([
    customerId,
    name,
    email,
    phone || '',
    formatDate(createdDate),
    0, 0, 0, 0, ''
  ]);
  
  return {
    customerId: customerId,
    name: name,
    email: email,
    phone: phone || '',
    createdDate: formatDate(createdDate),
    totalSessionsPurchased: 0,
    totalSessionsUsed: 0,
    totalSessionsRemaining: 0,
    totalSpent: 0,
    notes: '',
    isNew: true
  };
}

function getCustomerById(customerId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var data = customersSheet.getDataRange().getValues();
  
  for (var i = 1; i < data.length; i++) {
    if (data[i][0] === customerId) {
      return {
        customerId: data[i][0],
        name: data[i][1],
        email: data[i][2],
        phone: data[i][3],
        createdDate: data[i][4],
        totalSessionsPurchased: data[i][5],
        totalSessionsUsed: data[i][6],
        totalSessionsRemaining: data[i][7],
        totalSpent: data[i][8],
        notes: data[i][9]
      };
    }
  }
  return null;
}

function getCustomerProfile(data) {
  if (!data.email && !data.customerId) {
    return createResponse(false, 'Email vagy CustomerID szükséges');
  }
  
  var customer;
  if (data.customerId) {
    customer = getCustomerById(data.customerId);
  } else {
    var result = getOrCreateCustomer(data.name || '', data.email, data.phone || '');
    customer = result;
  }
  
  if (!customer) {
    return createResponse(false, 'Vásárló nem található');
  }
  
  // Get active packages
  var packages = getCustomerPackages(customer.customerId);
  
  // Get booking history
  var bookings = getCustomerBookings(customer.customerId);
  
  return createResponse(true, 'Profil betöltve', {
    customer: customer,
    activePackages: packages.filter(p => p.status === 'Active'),
    completedPackages: packages.filter(p => p.status === 'Completed'),
    recentBookings: bookings.slice(0, 10),
    allBookings: bookings
  });
}

function getAllCustomers() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var data = customersSheet.getDataRange().getValues();
  
  var customers = [];
  for (var i = 1; i < data.length; i++) {
    customers.push({
      customerId: data[i][0],
      name: data[i][1],
      email: data[i][2],
      phone: data[i][3],
      totalSessionsRemaining: data[i][7],
      totalSpent: data[i][8]
    });
  }
  
  return createResponse(true, 'Vásárlók listája', { customers: customers });
}

function getCustomerPackages(customerId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var packagesSheet = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
  var data = packagesSheet.getDataRange().getValues();
  
  var packages = [];
  for (var i = 1; i < data.length; i++) {
    if (data[i][1] === customerId) {
      packages.push({
        packageId: data[i][0],
        purchaseDate: data[i][3],
        serviceType: data[i][4],
        sessionsPurchased: data[i][5],
        sessionsUsed: data[i][6],
        sessionsRemaining: data[i][7],
        originalPrice: data[i][8],
        discountPercent: data[i][9],
        discountAmount: data[i][10],
        finalPrice: data[i][11],
        depositPaid: data[i][12],
        status: data[i][13]
      });
    }
  }
  return packages;
}

function getCustomerBookings(customerId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var data = bookingsSheet.getDataRange().getValues();
  
  var bookings = [];
  for (var i = 1; i < data.length; i++) {
    if (data[i][1] === customerId) {
      bookings.push({
        bookingId: data[i][0],
        service: data[i][3],
        date: data[i][4],
        time: data[i][5],
        status: data[i][6],
        packageId: data[i][7],
        sessionNumber: data[i][8]
      });
    }
  }
  return bookings.reverse(); // Most recent first
}

function findActivePackage(customerId, serviceType) {
  var packages = getCustomerPackages(customerId);
  
  for (var i = 0; i < packages.length; i++) {
    if (packages[i].status === 'Active' && packages[i].sessionsRemaining > 0) {
      // Check if service matches or package is for any service
      if (packages[i].serviceType === 'Bármely kezelés' || 
          packages[i].serviceType === serviceType ||
          !serviceType) {
        return packages[i];
      }
    }
  }
  return null;
}

function getPackageById(packageId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var packagesSheet = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
  var data = packagesSheet.getDataRange().getValues();
  
  for (var i = 1; i < data.length; i++) {
    if (data[i][0] === packageId) {
      return {
        packageId: data[i][0],
        customerId: data[i][1],
        sessionsPurchased: data[i][5],
        sessionsUsed: data[i][6],
        sessionsRemaining: data[i][7],
        status: data[i][13]
      };
    }
  }
  return null;
}

function updateCustomerSessionTotals(customerId) {
  var packages = getCustomerPackages(customerId);
  
  var totalPurchased = 0;
  var totalUsed = 0;
  var totalSpent = 0;
  
  for (var i = 0; i < packages.length; i++) {
    totalPurchased += packages[i].sessionsPurchased;
    totalUsed += packages[i].sessionsUsed;
    totalSpent += packages[i].finalPrice;
  }
  
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var data = customersSheet.getDataRange().getValues();
  
  for (var j = 1; j < data.length; j++) {
    if (data[j][0] === customerId) {
      customersSheet.getRange(j + 1, 6).setValue(totalPurchased);
      customersSheet.getRange(j + 1, 7).setValue(totalUsed);
      customersSheet.getRange(j + 1, 8).setValue(totalPurchased - totalUsed);
      customersSheet.getRange(j + 1, 9).setValue(totalSpent);
      break;
    }
  }
}

// ============================================
// Stripe Checkout Integration
// ============================================

// Stripe keys are read from Script Properties for security.
// Set these in Apps Script: Project Settings → Script properties
const STRIPE_SECRET_KEY = PropertiesService.getScriptProperties().getProperty('STRIPE_SECRET_KEY') || '';
const STRIPE_PUBLISHABLE_KEY = PropertiesService.getScriptProperties().getProperty('STRIPE_PUBLISHABLE_KEY') || '';
const STRIPE_WEBHOOK_SECRET = PropertiesService.getScriptProperties().getProperty('STRIPE_WEBHOOK_SECRET') || '';

const BANK_ACCOUNT = {
  HOLDER: 'Makra Edina',
  ACCOUNT: '10700581-73054012-51100005',
  BANK: 'CIB Bank'
};

function validateStripeKeys_() {
  if (!STRIPE_SECRET_KEY || STRIPE_SECRET_KEY.indexOf('REPLACE') !== -1) {
    return 'A Stripe titkos kulcs nincs beállítva. Nyisd meg a Google Apps Script → Projekt beállítások → Szkript tulajdonságai, és írd be a valódi STRIPE_SECRET_KEY értéket a Stripe Dashboardból.';
  }
  return null;
}

function generatePaymentReference_() {
  // Short code for bank transfer közlemény field (e.g. DM3K9F2)
  var rand = Math.random().toString(36).substring(2, 7).toUpperCase();
  return 'DM' + rand;
}

function ensurePendingBookingsSheet_() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var pendingSheet = ss.getSheetByName('PendingBookings');
  var headers = [
    'Timestamp', 'Name', 'Email', 'Phone', 'Service', 'Date', 'Time',
    'Notes', 'Recurring', 'RecurringType', 'RecurringCount', 'Amount',
    'Status', 'PaymentMethod', 'ReferenceId'
  ];

  if (!pendingSheet) {
    pendingSheet = ss.insertSheet('PendingBookings');
    pendingSheet.appendRow(headers);
    pendingSheet.getRange(1, 1, 1, headers.length).setFontWeight('bold');
    return pendingSheet;
  }

  var lastCol = Math.max(pendingSheet.getLastColumn(), 1);
  var existing = pendingSheet.getRange(1, 1, 1, lastCol).getValues()[0];
  if (lastCol < 15 || existing[13] !== 'PaymentMethod') {
    if (lastCol < 14) pendingSheet.getRange(1, 14).setValue('PaymentMethod');
    if (lastCol < 15) pendingSheet.getRange(1, 15).setValue('ReferenceId');
    pendingSheet.getRange(1, 1, 1, 15).setFontWeight('bold');
  }
  return pendingSheet;
}

function isSlotHeldByPending_(dateStr, timeStr) {
  var pendingSheet = ensurePendingBookingsSheet_();
  var data = pendingSheet.getDataRange().getValues();
  var activeStatuses = ['pending', 'awaiting_bank_transfer'];
  for (var i = 1; i < data.length; i++) {
    var status = String(data[i][12] || '');
    if (activeStatuses.indexOf(status) === -1) continue;
    if (data[i][5] === dateStr && data[i][6] === timeStr) {
      return true;
    }
  }
  return false;
}

function createStripeCheckout(data) {
  try {
    var stripeKeyError = validateStripeKeys_();
    if (stripeKeyError) {
      return createResponse(false, stripeKeyError);
    }

    // Validate required fields
    if (!data.name || !data.email) {
      return createHtmlResponse('<h1>Hiba</h1><p>Hiányzó kötelező mezők (név, email)</p>');
    }
    
    // Parse and validate amount
    var amount = parseInt(data.amount);
    
    // Safety check: Stripe minimum for HUF is 175 Ft.
    // Force minimum 3000 Ft (20% of cheapest service 15000 Ft)
    if (!amount || isNaN(amount) || amount < 175) {
      amount = 3000;
      Logger.log('Backend safety check triggered. Received: ' + data.amount + ', using: 3000 Ft');
    }
    
    // Additional safety: ensure minimum 3000 Ft
    if (amount < 3000) {
      amount = 3000;
      Logger.log('Amount was below 3000, forcing to 3000 Ft');
    }
    
    Logger.log('Creating Stripe checkout with amount: ' + amount + ' Ft');

    // Check if the requested time slot is still available before charging the customer
    if (data.date && data.time) {
      if (!isTimeSlotAvailable(data.date, data.time)) {
        return createResponse(false, 'Ez az időpont sajnos már foglalt. Kérjük válasszon másik időpontot!');
      }
      if (isSlotHeldByPending_(data.date, data.time)) {
        return createResponse(false, 'Ez az időpont jelenleg egy másik függő foglalás alatt van. Kérjük válassz másik időpontot!');
      }
    }
    
    var serviceName = data.service || 'Masszázs kezelés';
    var recurringText = data.recurring === 'yes' ? ' (' + data.recurringCount + ' alkalom)' : '';
    
    // Build form-encoded payload for Stripe API
    // Stripe requires application/x-www-form-urlencoded format
    // NOTE: HUF is a two-decimal currency in Stripe. Amounts must be in fillér (1/100 Forint).
    // Multiply Forint amount by 100. E.g. 3000 Ft → 300000 fillér.
    var payload = 
      'mode=payment' +
      '&line_items[0][price_data][currency]=huf' +
      '&line_items[0][price_data][product_data][name]=' + encodeURIComponent('Foglalási díj - ' + serviceName + recurringText) +
      '&line_items[0][price_data][product_data][description]=' + encodeURIComponent('Dunakeszi Masszázs - Angyali Szalon foglalási díj') +
      '&line_items[0][price_data][unit_amount]=' + (amount * 100) + // HUF is two-decimal: send fillér (Ft × 100)
      '&line_items[0][quantity]=1' +
      '&customer_email=' + encodeURIComponent(data.email) +
      '&success_url=' + encodeURIComponent(data.successUrl || 'https://www.dunakeszimasszazs.hu/booking-success') +
      '&cancel_url=' + encodeURIComponent(data.cancelUrl || 'https://www.dunakeszimasszazs.hu/booking-cancel') +
      '&metadata[customer_name]=' + encodeURIComponent(data.name) +
      '&metadata[customer_email]=' + encodeURIComponent(data.email) +
      '&metadata[service]=' + encodeURIComponent(data.service || '') +
      '&metadata[date]=' + encodeURIComponent(data.date || '') +
      '&metadata[time]=' + encodeURIComponent(data.time || '') +
      '&metadata[recurring]=' + encodeURIComponent(data.recurring || 'no') +
      '&metadata[recurring_count]=' + encodeURIComponent(data.recurringCount || '1');
    
    var options = {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer ' + STRIPE_SECRET_KEY,
        'Content-Type': 'application/x-www-form-urlencoded'
      },
      payload: payload,
      muteHttpExceptions: true // This allows us to see the full error response
    };
    
    var response = UrlFetchApp.fetch('https://api.stripe.com/v1/checkout/sessions', options);
    var responseCode = response.getResponseCode();
    var result = JSON.parse(response.getContentText());
    
    if (responseCode === 200 && result.url) {
      // Store pending booking data
      data.paymentMethod = 'stripe';
      storePendingBooking(data);
      
      // Return JSON with the URL instead of HTML
      // This is more reliable for AJAX/Fetch calls from the website
      return createResponse(true, 'Booking recorded, redirect to Stripe', { url: result.url });
    } else {
      // Log the full error for debugging
      Logger.log('Stripe error: ' + JSON.stringify(result));
      return createResponse(false, 'Hiba a Stripe checkout létrehozásakor: ' + (result.error ? result.error.message : 'Ismeretlen hiba (HTTP ' + responseCode + ')'));
    }
    
  } catch (error) {
    logError('createStripeCheckout', error);
    return createResponse(false, 'Hiba a Stripe checkout során: ' + error.message);
  }
}

/**
 * Debug helper: creates a Stripe Checkout session and returns the raw Stripe response as JSON.
 * Use this for testing via curl/postman. Does not store pending booking or redirect.
 */
function createStripeCheckoutDebug(data) {
  try {
    if (!data.email) {
      return createResponse(false, 'Missing required fields: email');
    }

    var amount = parseInt(data.amount);
    
    // Safety check: Stripe minimum for HUF is 175 Ft.
    // Force minimum 3000 Ft (20% of cheapest service 15000 Ft)
    if (!amount || isNaN(amount) || amount < 175) {
      amount = 3000;
      Logger.log('Debug backend safety check. Received: ' + data.amount + ', using: 3000 Ft');
    }
    
    // Additional safety: ensure minimum 3000 Ft
    if (amount < 3000) {
      amount = 3000;
    }
    
    var serviceName = data.service || 'Masszázs kezelés';
    var recurringText = data.recurring === 'yes' ? ' (' + data.recurringCount + ' alkalom)' : '';

    // NOTE: HUF is a two-decimal currency in Stripe. Amounts must be in fillér (1/100 Forint).
    var payload = 
      'mode=payment' +
      '&line_items[0][price_data][currency]=huf' +
      '&line_items[0][price_data][product_data][name]=' + encodeURIComponent('Foglalási díj - ' + serviceName + recurringText) +
      '&line_items[0][price_data][product_data][description]=' + encodeURIComponent('Dunakeszi Masszázs - Angyali Szalon foglalási díj') +
      '&line_items[0][price_data][unit_amount]=' + (amount * 100) + // HUF is two-decimal: send fillér (Ft × 100)
      '&line_items[0][quantity]=1' +
      '&customer_email=' + encodeURIComponent(data.email) +
      '&success_url=' + encodeURIComponent(data.successUrl || 'https://www.dunakeszimasszazs.hu/booking-success') +
      '&cancel_url=' + encodeURIComponent(data.cancelUrl || 'https://www.dunakeszimasszazs.hu/booking-cancel') +
      '&metadata[customer_name]=' + encodeURIComponent(data.name || '') +
      '&metadata[customer_email]=' + encodeURIComponent(data.email || '') +
      '&metadata[service]=' + encodeURIComponent(data.service || '') +
      '&metadata[date]=' + encodeURIComponent(data.date || '') +
      '&metadata[time]=' + encodeURIComponent(data.time || '') +
      '&metadata[recurring]=' + encodeURIComponent(data.recurring || 'no') +
      '&metadata[recurring_count]=' + encodeURIComponent(data.recurringCount || '1');

    var options = {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer ' + STRIPE_SECRET_KEY,
        'Content-Type': 'application/x-www-form-urlencoded'
      },
      payload: payload,
      muteHttpExceptions: true
    };

    var response = UrlFetchApp.fetch('https://api.stripe.com/v1/checkout/sessions', options);
    var responseCode = response.getResponseCode();
    var resultText = response.getContentText();
    var result = {};
    try { result = JSON.parse(resultText); } catch (e) { result = { raw: resultText }; }

    return createResponse(true, 'Stripe API response', { status: responseCode, body: result });
  } catch (err) {
    return createResponse(false, 'Debug error: ' + err.message);
  }
}

/**
 * Convenience helper to set Stripe keys into Script Properties from the editor.
 * Run this in the Apps Script editor after replacing the placeholder values with your keys.
 */
function setStripeProperties() {
  var props = PropertiesService.getScriptProperties();
  props.setProperties({
    STRIPE_SECRET_KEY: 'sk_test_REPLACE_WITH_YOURS',
    STRIPE_PUBLISHABLE_KEY: 'pk_test_REPLACE_WITH_YOURS',
    STRIPE_WEBHOOK_SECRET: 'whsec_REPLACE_WITH_YOURS'
  });
  Logger.log('Stripe script properties set (replace placeholders with real keys if needed).');
}

function storePendingBooking(data) {
  var pendingSheet = ensurePendingBookingsSheet_();
  var status = data.status || 'pending';
  var paymentMethod = data.paymentMethod || 'stripe';
  var referenceId = data.referenceId || '';

  pendingSheet.appendRow([
    new Date(),
    data.name,
    data.email,
    data.phone || '',
    data.service,
    data.date,
    data.time,
    data.notes || '',
    (data.recurring === true || data.recurring === 'yes' || data.recurring === 'true') ? 'yes' : 'no',
    data.recurringType || '',
    data.recurringCount || 1,
    data.amount,
    status,
    paymentMethod,
    referenceId
  ]);
}

// ============================================
// Bank Transfer Booking Flow
// ============================================
function createBankTransferBooking(data) {
  try {
    if (!data.name || !data.email || !data.service || !data.date || !data.time) {
      return createResponse(false, 'Hiányzó kötelező mezők (név, email, kezelés, dátum, időpont)');
    }

    var amount = parseInt(data.amount);
    if (!amount || isNaN(amount) || amount < 175) {
      amount = 3000;
    }
    if (amount < 3000) {
      amount = 3000;
    }

    if (!isTimeSlotAvailable(data.date, data.time)) {
      return createResponse(false, 'Ez az időpont sajnos már foglalt. Kérjük válassz másik időpontot!');
    }
    if (isSlotHeldByPending_(data.date, data.time)) {
      return createResponse(false, 'Ez az időpont jelenleg egy másik függő foglalás alatt van. Kérjük válassz másik időpontot!');
    }

    var referenceId = generatePaymentReference_();
    data.amount = amount;
    data.paymentMethod = 'bank_transfer';
    data.referenceId = referenceId;
    data.status = 'awaiting_bank_transfer';

    storePendingBooking(data);
    sendBankTransferNotificationToEdina_(data, referenceId, amount);
    sendBankTransferInstructionsToCustomer_(data, referenceId, amount);

    return createResponse(true, 'Banki átutalásos foglalás rögzítve', {
      referenceId: referenceId,
      amount: amount,
      bankAccount: BANK_ACCOUNT,
      booking: {
        name: data.name,
        email: data.email,
        service: data.service,
        date: data.date,
        time: data.time,
        amount: amount
      }
    });
  } catch (error) {
    logError('createBankTransferBooking', error);
    return createResponse(false, 'Hiba a banki átutalásos foglalás során: ' + error.message);
  }
}

function confirmBankTransferBooking(data) {
  try {
    if (!data.referenceId) {
      return createResponse(false, 'Hiányzó közlemény azonosító (referenceId)');
    }

    var pendingSheet = ensurePendingBookingsSheet_();
    var sheetData = pendingSheet.getDataRange().getValues();
    var targetRow = -1;
    var rowData = null;

    for (var i = sheetData.length - 1; i >= 1; i--) {
      if (sheetData[i][14] === data.referenceId && sheetData[i][12] === 'awaiting_bank_transfer') {
        targetRow = i + 1;
        rowData = sheetData[i];
        break;
      }
    }

    if (targetRow === -1) {
      return createResponse(false, 'Nem található ilyen közlemény azonosítóval függő banki átutalás, vagy már megerősítve lett.');
    }

    var bookingData = {
      name: rowData[1],
      email: rowData[2],
      phone: rowData[3],
      service: rowData[4],
      date: rowData[5],
      time: rowData[6],
      notes: rowData[7],
      recurring: rowData[8],
      recurringType: rowData[9],
      recurringCount: rowData[10],
      amount: rowData[11],
      usePackage: 'false'
    };

    if (!isTimeSlotAvailable(bookingData.date, bookingData.time)) {
      return createResponse(false, 'Az időpont már nem szabad a naptárban. Ellenőrizd a foglalást, és szükség esetén egyeztess az ügyféllel.');
    }

    var result = createBooking(bookingData);
    var resultText = result.getContent();
    var resultJson = JSON.parse(resultText);
    if (!resultJson.success) {
      return createResponse(false, resultJson.message || 'Hiba a foglalás létrehozásakor');
    }

    pendingSheet.getRange(targetRow, 13).setValue('confirmed');

    recordTransaction(
      new Date(),
      'Payment',
      '',
      bookingData.name,
      'Banki átutalás foglalási díj - ' + bookingData.service + ' (' + data.referenceId + ')',
      parseInt(bookingData.amount) || 0,
      'Bank Transfer',
      data.referenceId
    );

    return createResponse(true, 'Banki átutalás megerősítve, foglalás létrehozva!', {
      referenceId: data.referenceId,
      bookingIds: resultJson.data ? resultJson.data.bookingIds : []
    });
  } catch (error) {
    logError('confirmBankTransferBooking', error);
    return createResponse(false, 'Hiba a banki átutalás megerősítésekor: ' + error.message);
  }
}

function sendBankTransferNotificationToEdina_(data, referenceId, amount) {
  var subject = '🏦 Új banki átutalásos foglalás - ' + data.name + ' (' + referenceId + ')';
  var body = 'Kedves Edina!\n\n' +
    'Új banki átutalásos foglalás érkezett a weboldalról.\n' +
    'Az ügyfél értesítést kapott a közlemény azonosítóról.\n\n' +
    '═══════════════════════════════════════\n' +
    '👤 Név: ' + data.name + '\n' +
    '📞 Telefon: ' + (data.phone || 'Nincs megadva') + '\n' +
    '📧 Email: ' + data.email + '\n' +
    '💆 Kezelés: ' + data.service + '\n' +
    '📅 Dátum: ' + data.date + '\n' +
    '⏰ Időpont: ' + data.time + '\n' +
    '💰 Összeg: ' + amount.toLocaleString('hu-HU') + ' Ft\n' +
    '🔖 Közlemény: ' + referenceId + '\n' +
    '═══════════════════════════════════════\n\n' +
    'Amikor megérkezik az átutalás, nyisd meg az admin felületet:\n' +
    'https://www.dunakeszimasszazs.hu/admin → Függőben → "Befizetés megerősítése"\n\n' +
    'Addig az időpont függőben van, de még nincs a naptárban.';

  var html = buildSimpleHtml('Új banki átutalásos foglalás', '🏦',
    '<p style="font-size:14px;color:#555;">Kedves <strong>Edina</strong>! Új banki átutalásos foglalás érkezett.</p>' +
    htmlRows([
      ['👤 Név', data.name],
      ['📞 Telefon', data.phone || 'Nincs megadva'],
      ['📧 Email', data.email],
      ['💆 Kezelés', data.service],
      ['📅 Dátum', data.date],
      ['⏰ Időpont', data.time],
      ['💰 Összeg', amount.toLocaleString('hu-HU') + ' Ft'],
      ['🔖 Közlemény', '<strong style="color:#D4854A;font-size:16px;">' + referenceId + '</strong>']
    ]) +
    '<p style="font-size:13px;background:#fff7ed;border:1px solid #fdba74;border-radius:6px;padding:10px;margin-top:12px;">' +
    '⚠️ <strong>Fontos:</strong> A befizetés beérkezése után erősítsd meg az admin felületen: ' +
    '<a href="https://www.dunakeszimasszazs.hu/admin" style="color:#D4854A;">dunakeszimasszazs.hu/admin</a> → Függőben → Befizetés megerősítése</p>'
  );

  sendEmail_({
    to: 'dunakeszimasszor@gmail.com',
    subject: subject,
    body: body,
    htmlBody: html,
    name: 'Dunakeszi Masszázs - Admin',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
}

function sendBankTransferInstructionsToCustomer_(data, referenceId, amount) {
  var subject = 'Foglalás rögzítve - Banki átutalás szükséges (' + referenceId + ')';
  var body = 'Kedves ' + data.name + '!\n\n' +
    'Köszönöm a foglalást! A foglalásod rögzítve lett, és a véglegesítéshez kérjük a foglalási díj banki átutalását.\n\n' +
    '═══════════════════════════════════════\n' +
    '💆 Kezelés: ' + data.service + '\n' +
    '📅 Dátum: ' + data.date + '\n' +
    '⏰ Időpont: ' + data.time + '\n' +
    '💰 Összeg: ' + amount.toLocaleString('hu-HU') + ' Ft\n' +
    '═══════════════════════════════════════\n\n' +
    'BANKI ÁTUTALÁS ADATAI:\n' +
    'Számlatulajdonos: ' + BANK_ACCOUNT.HOLDER + '\n' +
    'Számlaszám: ' + BANK_ACCOUNT.ACCOUNT + '\n' +
    'Összeg: ' + amount.toLocaleString('hu-HU') + ' Ft\n' +
    'Közlemény (KÖTELEZŐ!): ' + referenceId + '\n\n' +
    '⚠️ Fontos: a közlemény mezőbe pontosan ezt írd be: ' + referenceId + '\n' +
    'Így tudom azonosítani a befizetésedet.\n\n' +
    'A foglalás a befizetés beérkezése után válik véglegessé. Visszaigazoló emailt küldök, amint megérkezik az összeg.\n\n' +
    '📍 Dunakeszi, Kolonics György utca 2/B — Kapucsengő: 1/43\n' +
    '📞 +36 30 487 7883\n\n' +
    'Üdvözlettel,\nMakra Edina\nDunakeszi Masszázs - Angyali Szalon';

  var html = buildSimpleHtml('Foglalás rögzítve – banki átutalás', '🏦',
    '<p style="font-size:14px;color:#555;">Kedves <strong>' + data.name + '</strong>! Köszönöm a foglalást.</p>' +
    htmlRows([
      ['💆 Kezelés', data.service],
      ['📅 Dátum', data.date],
      ['⏰ Időpont', data.time],
      ['💰 Összeg', amount.toLocaleString('hu-HU') + ' Ft']
    ]) +
    '<div style="background:#fff7ed;border:2px solid #D4854A;border-radius:8px;padding:16px;margin:16px 0;">' +
    '<p style="margin:0 0 8px;font-weight:bold;color:#4A3F35;">Banki átutalás adatai</p>' +
    htmlRows([
      ['Számlatulajdonos', BANK_ACCOUNT.HOLDER],
      ['Számlaszám', BANK_ACCOUNT.ACCOUNT],
      ['Összeg', amount.toLocaleString('hu-HU') + ' Ft'],
      ['Közlemény', '<strong style="color:#D4854A;font-size:18px;">' + referenceId + '</strong>']
    ]) +
    '</div>' +
    '<p style="font-size:13px;color:#555;">⚠️ A közlemény mezőbe <strong>pontosan</strong> a fenti azonosítót írd be, hogy azonosítani tudjam a befizetést.</p>' +
    '<p style="font-size:13px;color:#555;">A foglalás a befizetés beérkezése után válik véglegessé.</p>'
  );

  sendEmail_({
    to: data.email,
    subject: subject,
    body: body,
    htmlBody: html,
    name: 'Dunakeszi Masszázs - Angyali Szalon',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
}

// ============================================
// Get All Bookings & Pending Bookings (for Admin Panel)
// ============================================
function getAllBookings_() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var data = bookingsSheet.getDataRange().getValues();

  // Build customerId -> email lookup map
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var custData = customersSheet.getDataRange().getValues();
  var emailMap = {};
  for (var k = 1; k < custData.length; k++) {
    emailMap[custData[k][0]] = custData[k][2];
  }

  var bookings = [];
  for (var i = 1; i < data.length; i++) {
    bookings.push({
      bookingId: data[i][0],
      customerId: data[i][1],
      customerName: data[i][2],
      customerEmail: emailMap[data[i][1]] || '',
      service: data[i][3],
      date: typeof data[i][4] === 'string' ? data[i][4].slice(0,10) : formatDate(new Date(data[i][4])),
      time: formatTimeFromSheet(data[i][5]),
      status: data[i][6],
      packageId: data[i][7],
      sessionNumber: data[i][8],
      createdDate: typeof data[i][9] === 'string' ? data[i][9].slice(0,10) : (data[i][9] ? formatDate(new Date(data[i][9])) : '')
    });
  }
  return createResponse(true, 'Összes foglálás', { bookings: bookings.reverse() });
}

function getPendingBookings_() {
  var pendingSheet = ensurePendingBookingsSheet_();
  var data = pendingSheet.getDataRange().getValues();
  var bookings = [];
  for (var i = 1; i < data.length; i++) {
    var status = data[i][12];
    if (status === 'confirmed' || status === 'cancelled') continue;
    bookings.push({
      rowIndex: i + 1,
      timestamp: data[i][0],
      name: data[i][1],
      email: data[i][2],
      phone: data[i][3],
      service: data[i][4],
      date: typeof data[i][5] === 'string' ? data[i][5].slice(0, 10) : formatDate(new Date(data[i][5])),
      time: formatTimeFromSheet(data[i][6]),
      notes: data[i][7],
      recurring: data[i][8],
      recurringType: data[i][9],
      recurringCount: data[i][10],
      amount: data[i][11],
      status: status,
      paymentMethod: data[i][13] || 'stripe',
      referenceId: data[i][14] || ''
    });
  }
  return createResponse(true, 'Függő foglalások', { bookings: bookings.reverse() });
}

// ============================================
// Record Booking
// ============================================
function recordBooking(customerId, customerName, service, date, time, packageId, sessionNumber, calendarEventId, stripePaymentIntentId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  
  var bookingId = 'BK' + Date.now() + Math.random().toString(36).substr(2, 5);
  
  bookingsSheet.appendRow([
    bookingId,
    customerId,
    customerName,
    service,
    date,
    time,
    'Confirmed',
    packageId || '',
    sessionNumber || '',
    formatDate(new Date()),
    calendarEventId || '',
    stripePaymentIntentId || ''
  ]);
  SpreadsheetApp.flush(); // Ensure row is immediately visible to subsequent reads in the same execution
  
  return bookingId;
}

// ============================================
// Financial Tracking
// ============================================
function recordTransaction(date, type, customerId, customerName, description, amount, paymentMethod, relatedId) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var financialSheet = ss.getSheetByName(SHEETS.FINANCIAL);
  
  var transactionId = 'TXN' + Date.now();
  
  financialSheet.appendRow([
    transactionId,
    formatDate(date),
    type,
    customerId || '',
    customerName || '',
    description,
    amount,
    paymentMethod || '',
    relatedId || '',
    ''
  ]);
  
  return transactionId;
}

// ============================================
// Get All Packages
// ============================================
function getAllPackages_() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var packagesSheet = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
  if (!packagesSheet) return createResponse(false, 'SessionPackages lap nem található');
  var data = packagesSheet.getDataRange().getValues();
  var packages = [];
  for (var i = 1; i < data.length; i++) {
    if (!data[i][0]) continue; // skip empty rows
    packages.push({
      packageId: data[i][0],
      customerId: data[i][1],
      customerName: data[i][2],
      purchaseDate: data[i][3],
      serviceType: data[i][4],
      sessionsPurchased: data[i][5],
      sessionsUsed: data[i][6],
      sessionsRemaining: data[i][7],
      originalPrice: data[i][8],
      discountPercent: data[i][9],
      discountAmount: data[i][10],
      finalPrice: data[i][11],
      depositPaid: data[i][12],
      status: data[i][13],
      notes: data[i][14] || ''
    });
  }
  packages.reverse();
  return createResponse(true, 'Bérletek listája', { packages: packages });
}

// ============================================
// Self-Service Booking Management
// ============================================
// Customers can view, cancel, or request changes to their own bookings

function getMyBookings_(email) {
  if (!email) return createResponse(false, 'Email cím szükséges');

  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var customerData = customersSheet.getDataRange().getValues();

  var customerId = null;
  var customerName = '';
  for (var i = 1; i < customerData.length; i++) {
    if (customerData[i][2] === email) {
      customerId = customerData[i][0];
      customerName = customerData[i][1];
      break;
    }
  }

  if (!customerId) {
    return createResponse(false, 'Nem találtunk foglalást ezzel az email-lel. Ellenőrizd a foglaláskor használt címet.');
  }

  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var bookingsData = bookingsSheet.getDataRange().getValues();
  var today = new Date();
  today.setHours(0, 0, 0, 0);

  var bookings = [];
  for (var j = 1; j < bookingsData.length; j++) {
    if (bookingsData[j][1] === customerId && (bookingsData[j][6] === 'Confirmed' || bookingsData[j][6] === 'ChangeRequested')) {
      var bookingDate = new Date(bookingsData[j][4]);
      if (bookingDate >= today) {
        bookings.push({
          bookingId: bookingsData[j][0],
          service: bookingsData[j][3],
          date: typeof bookingsData[j][4] === 'string' ? bookingsData[j][4].slice(0,10) : formatDate(new Date(bookingsData[j][4])),
          time: formatTimeFromSheet(bookingsData[j][5]),
          status: bookingsData[j][6],
          calendarEventId: bookingsData[j][10] || ''
        });
      }
    }
  }

  bookings.sort(function(a, b) { return new Date(a.date) - new Date(b.date); });

  return createResponse(true, 'Foglalások betöltve', {
    bookings: bookings,
    customerName: customerName
  });
}

function selfCancelBooking_(data) {
  var bookingId = data.bookingId;
  var email = data.email;
  var reason = data.reason || '';

  if (!bookingId || !email) return createResponse(false, 'Hiányzó adatok');

  var ss = SpreadsheetApp.getActiveSpreadsheet();

  // Find customer by email
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var customerData = customersSheet.getDataRange().getValues();
  var customerId = null;
  var customerName = '';
  for (var i = 1; i < customerData.length; i++) {
    if (customerData[i][2] === email) {
      customerId = customerData[i][0];
      customerName = customerData[i][1];
      break;
    }
  }
  if (!customerId) return createResponse(false, 'Email cím nem található');

  // Find and verify the booking
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var bookingsData = bookingsSheet.getDataRange().getValues();
  var bookingRow = -1;
  var bookingService = '', bookingDate = '', bookingTime = '';

  for (var j = 1; j < bookingsData.length; j++) {
    if (bookingsData[j][0] === bookingId && bookingsData[j][1] === customerId) {
      if (bookingsData[j][6] !== 'Confirmed') {
        return createResponse(false, 'Ez a foglalás már nem aktív');
      }
      bookingRow = j + 1;
      bookingService = bookingsData[j][3];
      bookingDate = typeof bookingsData[j][4] === 'string' ? bookingsData[j][4].slice(0,10) : formatDate(new Date(bookingsData[j][4]));
      bookingTime = formatTimeFromSheet(bookingsData[j][5]);
      var calendarEventId = bookingsData[j][10] || '';
      var stripePaymentIntentId = bookingsData[j][11] || '';
      break;
    }
  }

  if (bookingRow === -1) return createResponse(false, 'Foglalás nem található');

  // Enforce 24h cancellation policy
  try {
    var bMs = new Date(bookingDate + 'T' + bookingTime + ':00').getTime();
    if ((bMs - Date.now()) < 24 * 60 * 60 * 1000) {
      return createResponse(false, 'A foglalást legkésőbb 24 órával korábban lehet lemondani.');
    }
  } catch(e) { /* date parsing edge case, continue */ }

  // Mark as cancelled in sheet
  bookingsSheet.getRange(bookingRow, 7).setValue('Cancelled');
  SpreadsheetApp.flush();
  var calendarDeleted = false;
  try {
    calendarDeleted = deleteCalendarEvent_(calendarEventId, bookingDate, bookingTime, customerName);
  } catch (calErr) {
    Logger.log('Calendar deletion skipped: ' + calErr);
  }

  // Attempt Stripe refund if we have a payment intent ID
  var refundStatus = 'Nincs Stripe fizetési adat – manuális visszatérítés szükséges';
  var refundAmount = '';
  if (stripePaymentIntentId) {
    try {
      var STRIPE_SECRET_KEY = PropertiesService.getScriptProperties().getProperty('STRIPE_SECRET_KEY');
      var refundResp = UrlFetchApp.fetch('https://api.stripe.com/v1/refunds', {
        method: 'POST',
        headers: { 'Authorization': 'Bearer ' + STRIPE_SECRET_KEY, 'Content-Type': 'application/x-www-form-urlencoded' },
        payload: 'payment_intent=' + encodeURIComponent(stripePaymentIntentId),
        muteHttpExceptions: true
      });
      var refundJson = JSON.parse(refundResp.getContentText());
      if (refundJson.id) {
        refundAmount = (refundJson.amount / 100).toLocaleString() + ' Ft';
        refundStatus = '✅ STRIPE VISSZATÉRÍTÉS SIKERES – ' + refundAmount + ' (refund ID: ' + refundJson.id + ')';
        Logger.log('Stripe refund issued: ' + refundJson.id + ' for ' + stripePaymentIntentId);
      } else {
        refundStatus = '⚠️ Stripe visszatérítés sikertelen: ' + JSON.stringify(refundJson) + ' – manuális visszatérítés szükséges!';
        Logger.log('Stripe refund failed: ' + JSON.stringify(refundJson));
      }
    } catch (refErr) {
      refundStatus = '⚠️ Stripe API hiba: ' + refErr + ' – manuális visszatérítés szükséges!';
      Logger.log('Stripe refund error: ' + refErr);
    }
  }

  // Notify Edina
  var edinaSubject = '🔴 Ügyfél mondta le – ' + customerName + ' (' + bookingDate + ' ' + bookingTime + ')';
  var edinaBody = 'Kedves Edina!\n\n';
  edinaBody += customerName + ' lemondta a foglalását a weboldalon keresztül:\n\n';
  edinaBody += '═══════════════════════════════════════\n';
  edinaBody += '👤 Név: ' + customerName + '\n';
  edinaBody += '📧 Email: ' + email + '\n';
  edinaBody += '💆 Kezelés: ' + bookingService + '\n';
  edinaBody += '📅 Dátum: ' + bookingDate + '\n';
  edinaBody += '⏰ Időpont: ' + bookingTime + '\n';
  if (reason) edinaBody += '📝 Indoklás: ' + reason + '\n';
  edinaBody += '═══════════════════════════════════════\n\n';
  edinaBody += '💳 VISSZATÉRÍTÉS: ' + refundStatus + '\n\n';
  if (calendarDeleted) {
    edinaBody += '✅ A Google Naptár esemény automatikusan törölve.';
  } else {
    edinaBody += '⚠️ A naptári esemény nem volt automán törölhető – kérjük ellenőrizd manuálisan a Google Naptárt!';
  }

  var cancelEdinaHtml = buildSimpleHtml('Foglalás lemondva', '🔴',
    '<p style="font-size:14px;color:#555;">Kedves Edina! ' + customerName + ' lemondta a foglalását.</p>' +
    htmlRows([['👤 Név', customerName], ['📧 Email', email], ['💪 Kezelés', bookingService], ['📅 Dátum', bookingDate], ['⏰ Időpont', bookingTime], ['📝 Indoklás', reason || '–']]) +
    '<p style="font-size:13px;background:#fff8f0;border:1px solid #fcd9b0;border-radius:6px;padding:10px;margin-top:12px;">💳 <strong>Visszatérítés:</strong> ' + refundStatus + '</p>' +
    '<p style="font-size:13px;color:#555;margin-top:8px;">' + (calendarDeleted ? '✅ Google Naptár esemény automatikusan törölve.' : '⚠️ Naptári eseményt kérjük ellenőrizd manuálisan!') + '</p>'
  );
  sendEmail_({ to: 'dunakeszimasszor@gmail.com', subject: edinaSubject, body: edinaBody, htmlBody: cancelEdinaHtml, name: 'Dunakeszi Masszázs - Admin', replyTo: 'dunakeszimasszor@gmail.com' });

  // Confirm to customer
  var custSubject = 'Foglalás lemondva – Dunakeszi Masszázs';
  var custBody = 'Kedves ' + customerName + '!\n\n';
  custBody += 'Foglalásod sikeresen lemondva:\n\n';
  custBody += '═══════════════════════════════════════\n';
  custBody += '💆 Kezelés: ' + bookingService + '\n';
  custBody += '📅 Dátum: ' + bookingDate + '\n';
  custBody += '⏰ Időpont: ' + bookingTime + '\n';
  custBody += '═══════════════════════════════════════\n\n';
  if (stripePaymentIntentId) {
    if (refundStatus.indexOf('SIKERES') !== -1) {
      custBody += '💳 A befizetett foglalási díj (' + refundAmount + ') visszautalásra kerül a bankkártyádra.\n';
      custBody += '   Az összeg általában 5–10 munkanapon belül megjelenik a számládon.\n\n';
    } else {
      custBody += '💳 A visszatérítéssel kapcsolatban hamarosan felvesszük veled a kapcsolatot.\n\n';
    }
  }
  custBody += 'Ha új időpontot szeretnél foglalni:\n';
  custBody += '🌐 https://dunakeszimasszazs.hu/#idopont\n\n';
  custBody += 'Üdvözlettel,\nMakra Edina\nDunakeszi Masszázs - Angyali Szalon\n📞 +36 30 487 7883';

  var refundNote = stripePaymentIntentId
    ? (refundStatus.indexOf('SIKERES') !== -1
        ? '<p style="font-size:13px;background:#f0fdf4;border:1px solid #86efac;border-radius:6px;padding:10px;">💳 A befizetett foglalási díj (' + refundAmount + ') visszautalásra kerül a bankkártyádra (5–10 munkanap).</p>'
        : '<p style="font-size:13px;background:#fff8f0;border:1px solid #fcd9b0;border-radius:6px;padding:10px;">💳 A visszatérítéssel kapcsolatban hamarosan felvesszük veled a kapcsolatot.</p>')
    : '';
  var cancelCustHtml = buildSimpleHtml('Foglalás lemondva', '✅',
    '<p style="font-size:14px;color:#555;">Kedves <strong>' + customerName + '</strong>! Foglalásod sikeresen lemondva.</p>' +
    htmlRows([['💪 Kezelés', bookingService], ['📅 Dátum', bookingDate], ['⏰ Időpont', bookingTime]]) +
    refundNote +
    '<p style="font-size:13px;color:#555;margin-top:12px;">Új időpontot bármikor foglalhatsz: <a href="https://dunakeszimasszazs.hu/#idopont" style="color:#D4854A;">dunakeszimasszazs.hu</a></p>'
  );
  sendEmail_({ to: email, subject: custSubject, body: custBody, htmlBody: cancelCustHtml, name: 'Dunakeszi Masszázs - Angyali Szalon', replyTo: 'dunakeszimasszor@gmail.com' });

  return createResponse(true, 'Foglalásod sikeresen lemondva! Visszaigazolást küldünk emailben.');
}

function selfRequestChange_(data) {
  var bookingId = data.bookingId;
  var email = data.email;
  var newDate = data.newDate;
  var newTime = data.newTime;
  var notes = data.notes || '';

  if (!bookingId || !email || !newDate || !newTime) return createResponse(false, 'Hiányzó adatok');

  var ss = SpreadsheetApp.getActiveSpreadsheet();

  // Find customer by email
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var customerData = customersSheet.getDataRange().getValues();
  var customerId = null;
  var customerName = '';
  for (var i = 1; i < customerData.length; i++) {
    if (customerData[i][2] === email) {
      customerId = customerData[i][0];
      customerName = customerData[i][1];
      break;
    }
  }
  if (!customerId) return createResponse(false, 'Email cím nem található');

  // Find the booking
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var bookingsData = bookingsSheet.getDataRange().getValues();
  var bookingService = '', bookingDate = '', bookingTime = '', calEventId = '';
  var bookingRow = -1;

  for (var j = 1; j < bookingsData.length; j++) {
    if (bookingsData[j][0] === bookingId && bookingsData[j][1] === customerId) {
      bookingService = bookingsData[j][3];
      bookingDate = typeof bookingsData[j][4] === 'string' ? bookingsData[j][4].slice(0,10) : formatDate(new Date(bookingsData[j][4]));
      bookingTime = formatTimeFromSheet(bookingsData[j][5]);
      calEventId = bookingsData[j][10] || '';
      bookingRow = j + 1;
      break;
    }
  }

  if (!bookingDate) return createResponse(false, 'Foglalás nem található');

  // Enforce 24h change policy on the OLD booking
  try {
    var bMs = new Date(bookingDate + 'T' + bookingTime + ':00').getTime();
    if ((bMs - Date.now()) < 24 * 60 * 60 * 1000) {
      return createResponse(false, 'A foglalást legkésőbb 24 órával korábban lehet módosítani.');
    }
  } catch(e) { /* date parsing edge case, continue */ }

  // Check if customer already has another booking at the requested new date+time
  for (var k = 1; k < bookingsData.length; k++) {
    if (bookingsData[k][0] !== bookingId && bookingsData[k][1] === customerId &&
        (bookingsData[k][6] === 'Confirmed' || bookingsData[k][6] === 'ChangeRequested')) {
      var chkDate = typeof bookingsData[k][4] === 'string' ? bookingsData[k][4].slice(0,10) : formatDate(new Date(bookingsData[k][4]));
      var chkTime = formatTimeFromSheet(bookingsData[k][5]);
      if (chkDate === newDate && chkTime === newTime) {
        return createResponse(false, 'Ezen az időponton már van aktív foglalásod! Kérjük válassz másik időpontot.');
      }
    }
  }

  // Check the new slot is actually free in the calendar
  if (!isTimeSlotAvailable(newDate, newTime)) {
    return createResponse(false, 'Ez az időpont sajnos már foglalt. Kérjük válassz másik időpontot.');
  }

  // ── Fully automatic: delete old calendar event, create new one ──
  try {
    deleteCalendarEvent_(calEventId, bookingDate, bookingTime, customerName);
  } catch (delErr) {
    Logger.log('Old calendar event deletion skipped: ' + delErr);
  }

  var newCalEventId = '';
  try {
    var calendar = CalendarApp.getDefaultCalendar();
    var dateParts = newDate.split('-');
    var timeParts = newTime.split(':');
    var newStart = new Date(
      parseInt(dateParts[0]), parseInt(dateParts[1]) - 1, parseInt(dateParts[2]),
      parseInt(timeParts[0]), parseInt(timeParts[1])
    );
    var newEnd = new Date(newStart.getTime() + 75 * 60 * 1000);
    var newEvent = calendar.createEvent('Masszázs - ' + customerName + ' - ' + bookingService, newStart, newEnd, {
      description: 'Módosított foglalás\nÜgyfél: ' + customerName + '\nEmail: ' + email + '\nKezelés: ' + bookingService,
      location: 'Angyali Szalon, Dunakeszi'
    });
    newEvent.setColor('6'); // orange
    newCalEventId = newEvent.getId();
  } catch (calErr) {
    Logger.log('New calendar event creation skipped: ' + calErr);
  }

  // Update booking: new date, time, status = Confirmed, new calendar event ID
  if (bookingRow > 0) {
    bookingsSheet.getRange(bookingRow, 5).setValue(newDate);
    bookingsSheet.getRange(bookingRow, 6).setValue(newTime);
    bookingsSheet.getRange(bookingRow, 7).setValue('Confirmed');
    if (newCalEventId) bookingsSheet.getRange(bookingRow, 11).setValue(newCalEventId);
    SpreadsheetApp.flush();
  }

  // Notify Edina (FYI — no action needed)
  var edinaSubject = '✅ Időpont módosítva – ' + customerName;
  var edinaBody = 'Kedves Edina!\n\n' + customerName + ' időpontja automatikusan módosítva:\n\n' +
    '❌ Régi: ' + bookingDate + ' ' + bookingTime + '\n' +
    '✅ Új: ' + newDate + ' ' + newTime + '\n' +
    '💆 Kezelés: ' + bookingService + '\n' +
    (notes ? '📝 Megjegyzés: ' + notes + '\n' : '') +
    '\nA Google Naptár automatikusan frissítve.';
  var changeEdinaHtml = buildSimpleHtml('Időpont módosítva (automatikus)', '✅',
    '<p style="font-size:14px;color:#555;">Kedves Edina! ' + customerName + ' időpontja automatikusan módosítva — nincs teendőd.</p>' +
    htmlRows([['👤 Név', customerName], ['📧 Email', email], ['💪 Kezelés', bookingService]]) +
    '<div style="display:flex;gap:12px;margin:12px 0;">' +
    '<div style="flex:1;background:#fff0f0;border:1px solid #fca5a5;border-radius:6px;padding:10px;font-size:13px;"><strong>❌ Régi</strong><br>' + bookingDate + ' ' + bookingTime + '</div>' +
    '<div style="flex:1;background:#f0fdf4;border:1px solid #86efac;border-radius:6px;padding:10px;font-size:13px;"><strong>✅ Új</strong><br>' + newDate + ' ' + newTime + '</div>' +
    '</div>' +
    (notes ? '<p style="font-size:13px;color:#555;">📝 Megjegyzés: ' + notes + '</p>' : '') +
    '<p style="font-size:13px;color:#555;margin-top:8px;">🗓️ Google Naptár automatikusan frissítve.</p>'
  );
  sendEmail_({ to: 'dunakeszimasszor@gmail.com', subject: edinaSubject, body: edinaBody, htmlBody: changeEdinaHtml, name: 'Dunakeszi Masszázs - Admin', replyTo: 'dunakeszimasszor@gmail.com' });

  // Confirm to customer immediately
  var custSubject = '✅ Időpont módosítva – Dunakeszi Masszázs';
  var custBody = 'Kedves ' + customerName + '!\n\n' +
    'Foglalásod sikeresen módosítva:\n\n' +
    '💆 Kezelés: ' + bookingService + '\n' +
    '✅ Új időpont: ' + newDate + ' ' + newTime + '\n\n' +
    '📍 Dunakeszi, Kolonics György utca 2/B – Kapucsengő: 1/43\n\n' +
    'Üdvözlettel,\nMakra Edina\n📞 +36 30 487 7883';
  var changeCustHtml = buildSimpleHtml('Időpont sikeresen módosítva', '✅',
    '<p style="font-size:14px;color:#555;">Kedves <strong>' + customerName + '</strong>! Foglalásod módosítva.</p>' +
    htmlRows([['💪 Kezelés', bookingService], ['✅ Új időpont', newDate + ' ' + newTime], ['📍 Cím', 'Dunakeszi, Kolonics György utca 2/B'], ['🔔 Kapucsengő', '1/43']]) +
    (notes ? '<p style="font-size:13px;color:#555;margin-top:8px;">📝 Megjegyzés: ' + notes + '</p>' : '') +
    '<p style="font-size:13px;color:#555;margin-top:12px;">Ha kérdésed van: <a href="tel:+36304877883" style="color:#D4854A;">+36 30 487 7883</a></p>'
  );
  sendEmail_({ to: email, subject: custSubject, body: custBody, htmlBody: changeCustHtml, name: 'Dunakeszi Masszázs - Angyali Szalon', replyTo: 'dunakeszimasszor@gmail.com' });

  return createResponse(true, 'Időpontod sikeresen módosítva! Visszaigazolást küldtünk emailben.');
}

// ============================================
// Calendar Helper Functions
// ============================================

// Tries to delete a Google Calendar event by its stored ID.
// Falls back to searching by date/time window + customer name if ID is missing.
function deleteCalendarEvent_(calendarEventId, bookingDate, bookingTime, customerName) {
  var calendar;
  try { calendar = CalendarApp.getDefaultCalendar(); } catch(e) {
    Logger.log('CalendarApp unavailable in deleteCalendarEvent_: ' + e);
    return false;
  }

  // Primary: delete by stored event ID
  if (calendarEventId) {
    try {
      var evt = calendar.getEventById(calendarEventId);
      if (evt) {
        evt.deleteEvent();
        Logger.log('Calendar event deleted by ID: ' + calendarEventId);
        return true;
      }
    } catch (e) {
      Logger.log('Could not delete by ID, falling back to search: ' + e);
    }
  }

  // Fallback: search for events within ±2 minutes of the booking slot
  try {
    var parts = bookingTime.split(':');
    var start = new Date(bookingDate + 'T' + (parts[0] || '00') + ':' + (parts[1] || '00') + ':00');
    var searchStart = new Date(start.getTime() - 2 * 60000);
    var searchEnd   = new Date(start.getTime() + 2 * 60000);
    var events = calendar.getEvents(searchStart, searchEnd);
    for (var i = 0; i < events.length; i++) {
      var title = events[i].getTitle();
      if (title.indexOf(customerName) !== -1 || title.indexOf('Masszázs') !== -1) {
        events[i].deleteEvent();
        Logger.log('Calendar event deleted by search: ' + title);
        return true;
      }
    }
  } catch (e) {
    Logger.log('Fallback calendar search failed: ' + e);
  }

  Logger.log('Calendar event not found for: ' + customerName + ' ' + bookingDate + ' ' + bookingTime);
  return false;
}

// Flags a Google Calendar event to visually show a pending change request.
// Updates the event title and adds the new requested date/time to the description.
function flagCalendarEventForChange_(calendarEventId, bookingDate, bookingTime, customerName, newDate, newTime) {
  var calendar;
  try { calendar = CalendarApp.getDefaultCalendar(); } catch(e) {
    Logger.log('CalendarApp unavailable in flagCalendarEventForChange_: ' + e);
    return false;
  }
  var changeNote = 'MÓDOSÍTÁS KÉRT: ' + newDate + ' ' + newTime;

  // Primary: update by stored event ID
  if (calendarEventId) {
    try {
      var evt = calendar.getEventById(calendarEventId);
      if (evt) {
        evt.setTitle('⚠️ ' + changeNote + ' | ' + evt.getTitle());
        var desc = evt.getDescription() || '';
        evt.setDescription('⚠️ Ügyfél módosítást kért!\nKért új időpont: ' + newDate + ' ' + newTime + '\n\n' + desc);
        Logger.log('Calendar event flagged by ID: ' + calendarEventId);
        return true;
      }
    } catch (e) {
      Logger.log('Could not flag by ID, falling back to search: ' + e);
    }
  }

  // Fallback: search ±2 minutes
  try {
    var parts = bookingTime.split(':');
    var start = new Date(bookingDate + 'T' + (parts[0] || '00') + ':' + (parts[1] || '00') + ':00');
    var searchStart = new Date(start.getTime() - 2 * 60000);
    var searchEnd   = new Date(start.getTime() + 2 * 60000);
    var events = calendar.getEvents(searchStart, searchEnd);
    for (var i = 0; i < events.length; i++) {
      var title = events[i].getTitle();
      if (title.indexOf(customerName) !== -1) {
        events[i].setTitle('⚠️ ' + changeNote + ' | ' + title);
        var desc = events[i].getDescription() || '';
        events[i].setDescription('⚠️ Ügyfél módosítást kért!\nKért új időpont: ' + newDate + ' ' + newTime + '\n\n' + desc);
        Logger.log('Calendar event flagged by search: ' + title);
        return true;
      }
    }
  } catch (e) {
    Logger.log('Fallback flag search failed: ' + e);
  }

  return false;
}

// ============================================
// P&L Dashboard
// ============================================
// Internal helper – returns a plain object (no ContentService wrapping).
// Used by both getPnL() and getDashboardData().
function getPnLData_(month, year) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var financialSheet = ss.getSheetByName(SHEETS.FINANCIAL);
  var txnData = financialSheet.getDataRange().getValues();
  
  var monthlyIncome = 0;
  var monthlyDeposits = 0;
  var estimatedFullRevenue = 0;
  
  var transactions = [];
  
  for (var i = 1; i < txnData.length; i++) {
    var txnDate = new Date(txnData[i][1]);
    if (txnDate.getMonth() + 1 === parseInt(month) && txnDate.getFullYear() === parseInt(year)) {
      var amount = txnData[i][6];
      var type = txnData[i][2];
      
      if (type === 'Income' || type === 'Payment') {
        monthlyIncome += amount;
        // Payment = 20% deposit → estimate full session value
        if (type === 'Payment') {
          estimatedFullRevenue += amount * 5;
        } else {
          estimatedFullRevenue += amount;
        }
      } else if (type === 'Deposit') {
        monthlyDeposits += amount;
      }
      
      transactions.push({
        date: txnData[i][1],
        type: type,
        customer: txnData[i][4],
        description: txnData[i][5],
        amount: amount
      });
    }
  }
  
  // Get session stats
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var bookingsData = bookingsSheet.getDataRange().getValues();
  var sessionsCompleted = 0;
  
  for (var j = 1; j < bookingsData.length; j++) {
    var bookingDate = new Date(bookingsData[j][4]);
    if (bookingDate.getMonth() + 1 === parseInt(month) && 
        bookingDate.getFullYear() === parseInt(year) &&
        bookingsData[j][6] === 'Confirmed') {
      sessionsCompleted++;
    }
  }
  
  return {
    month: month,
    year: year,
    totalIncome: monthlyIncome,
    totalDeposits: monthlyDeposits,
    estimatedFullRevenue: estimatedFullRevenue,
    outstanding: monthlyIncome - monthlyDeposits,
    sessionsCompleted: sessionsCompleted,
    transactions: transactions
  };
}

// Range-based P&L (used for day and week views)
function getPnLDataRange_(startDateStr, endDateStr) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var financialSheet = ss.getSheetByName(SHEETS.FINANCIAL);
  var txnData = financialSheet.getDataRange().getValues();

  var startDate = new Date(startDateStr);
  startDate.setHours(0, 0, 0, 0);
  var endDate = new Date(endDateStr);
  endDate.setHours(23, 59, 59, 999);

  var totalIncome = 0;
  var totalDeposits = 0;
  var estimatedFullRevenue = 0;
  var transactions = [];

  for (var i = 1; i < txnData.length; i++) {
    var txnDate = new Date(txnData[i][1]);
    if (txnDate >= startDate && txnDate <= endDate) {
      var amount = txnData[i][6];
      var type = txnData[i][2];
      if (type === 'Income' || type === 'Payment') {
        totalIncome += amount;
        if (type === 'Payment') {
          estimatedFullRevenue += amount * 5;
        } else {
          estimatedFullRevenue += amount;
        }
      } else if (type === 'Deposit') {
        totalDeposits += amount;
      }
      transactions.push({
        date: txnData[i][1],
        type: type,
        customer: txnData[i][4],
        description: txnData[i][5],
        amount: amount
      });
    }
  }

  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var bookingsData = bookingsSheet.getDataRange().getValues();
  var sessionsCompleted = 0;
  for (var j = 1; j < bookingsData.length; j++) {
    var bookingDate = new Date(bookingsData[j][4]);
    if (bookingDate >= startDate && bookingDate <= endDate && bookingsData[j][6] === 'Confirmed') {
      sessionsCompleted++;
    }
  }

  return {
    totalIncome: totalIncome,
    totalDeposits: totalDeposits,
    estimatedFullRevenue: estimatedFullRevenue,
    outstanding: totalIncome - totalDeposits,
    sessionsCompleted: sessionsCompleted,
    transactions: transactions
  };
}

function getPnL(data) {
  var mode = data.mode || 'month';
  if (mode === 'day' && data.date) {
    return createResponse(true, 'P&L adatok', getPnLDataRange_(data.date, data.date));
  } else if (mode === 'week' && data.weekStart) {
    var startDate = new Date(data.weekStart);
    var endDate = new Date(startDate);
    endDate.setDate(endDate.getDate() + 6);
    var endStr = Utilities.formatDate(endDate, Session.getScriptTimeZone(), 'yyyy-MM-dd');
    return createResponse(true, 'P&L adatok', getPnLDataRange_(data.weekStart, endStr));
  } else {
    var month = data.month || new Date().getMonth() + 1;
    var year = data.year || new Date().getFullYear();
    return createResponse(true, 'P&L adatok', getPnLData_(month, year));
  }
}

function getDashboardData() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  
  // Get customer count
  var customersSheet = ss.getSheetByName(SHEETS.CUSTOMERS);
  var customerCount = customersSheet.getLastRow() - 1;
  
  // Get active packages count
  var packagesSheet = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
  var packagesData = packagesSheet.getDataRange().getValues();
  var activePackages = 0;
  var totalSessionsRemaining = 0;
  
  for (var i = 1; i < packagesData.length; i++) {
    if (packagesData[i][13] === 'Active') {
      activePackages++;
      totalSessionsRemaining += packagesData[i][7];
    }
  }
  
  // Get today's bookings
  var bookingsSheet = ss.getSheetByName(SHEETS.BOOKINGS);
  var bookingsData = bookingsSheet.getDataRange().getValues();
  var today = formatDate(new Date());
  var todaysBookings = [];
  
  for (var j = 1; j < bookingsData.length; j++) {
    if (bookingsData[j][4] === today) {
      todaysBookings.push({
        customer: bookingsData[j][2],
        service: bookingsData[j][3],
        time: bookingsData[j][5]
      });
    }
  }
  
  // Get current month P&L
  var pnlData = getPnLData_(new Date().getMonth() + 1, new Date().getFullYear());
  
  return createResponse(true, 'Dashboard adatok', {
    customerCount: customerCount,
    activePackages: activePackages,
    totalSessionsRemaining: totalSessionsRemaining,
    todaysBookings: todaysBookings,
    monthlyPnL: pnlData
  });
}

// ============================================
// Cancellation & Change Handlers
// ============================================
function handleCancellation(data) {
  if (!data.clientName || !data.clientEmail || !data.appointmentDate || !data.appointmentTime) {
    return createResponse(false, 'Hiányzó kötelező mezők');
  }
  
  // Send cancellation email to customer
  var subject = 'Időpont lemondva - Dunakeszi Masszázs';
  var body = 'Kedves ' + data.clientName + '!\n\n' +
             'Értesítlek, hogy az alábbi időpontod lemondásra került:\n\n' +
             '═══════════════════════════════════════\n' +
             '💆 Kezelés: ' + (data.service || 'Nincs megadva') + '\n' +
             '📅 Dátum: ' + data.appointmentDate + '\n' +
             '⏰ Időpont: ' + data.appointmentTime + '\n' +
             '═══════════════════════════════════════\n\n';
  
  if (data.reason) {
    body += 'Indoklás: ' + data.reason + '\n\n';
  }
  
  body += 'Ha szeretnél új időpontot foglalni, kérem, látogass el a weboldalra vagy hívj telefonon.\n\n' +
          '📞 +36 30 487 7883\n' +
          '🌐 https://dunakeszimasszazs.hu\n\n' +
          'Üdvözlettel,\n' +
          'Makra Edina\n' +
          'Dunakeszi Masszázs - Angyali Szalon';
  
  sendEmail_({
    to: data.clientEmail,
    subject: subject,
    body: body,
    name: 'Dunakeszi Masszázs - Angyali Szalon',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
  
  // Send notification to Edina
  var edinaSubject = 'Időpont lemondva - ' + data.clientName;
  var edinaBody = 'Kedves Edina!\n\n' +
                  'Egy időpont lemondásra került:\n\n' +
                  '═══════════════════════════════════════\n' +
                  '👤 Név: ' + data.clientName + '\n' +
                  '📧 Email: ' + data.clientEmail + '\n' +
                  '💆 Kezelés: ' + (data.service || 'Nincs megadva') + '\n' +
                  '📅 Dátum: ' + data.appointmentDate + '\n' +
                  '⏰ Időpont: ' + data.appointmentTime + '\n' +
                  '═══════════════════════════════════════\n\n';
  
  if (data.reason) {
    edinaBody += 'Indoklás: ' + data.reason + '\n\n';
  }
  
  edinaBody += 'Ne felejtsd el törölni az időpontot a Google Naptárból!';
  
  sendEmail_({
    to: 'dunakeszimasszor@gmail.com',
    subject: edinaSubject,
    body: edinaBody,
    name: 'Dunakeszi Masszázs - Admin',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
  
  return createResponse(true, 'Lemondási értesítés elküldve!');
}

function handleChange(data) {
  if (!data.clientName || !data.clientEmail || !data.appointmentDate || !data.appointmentTime || !data.newDate || !data.newTime) {
    return createResponse(false, 'Hiányzó kötelező mezők');
  }
  
  // Send change email to customer
  var subject = 'Időpont módosítva - Dunakeszi Masszázs';
  var body = 'Kedves ' + data.clientName + '!\n\n' +
             'Értesítlek, hogy az időpontod módosításra került:\n\n' +
             '═══════════════════════════════════════\n' +
             '❌ EREDETI IDŐPONT:\n' +
             '📅 Dátum: ' + data.appointmentDate + '\n' +
             '⏰ Időpont: ' + data.appointmentTime + '\n\n' +
             '✅ ÚJ IDŐPONT:\n' +
             '📅 Dátum: ' + data.newDate + '\n' +
             '⏰ Időpont: ' + data.newTime + '\n' +
             '💆 Kezelés: ' + (data.service || 'Nincs megadva') + '\n' +
             '═══════════════════════════════════════\n\n' +
             '📍 CÍM ÉS MEGKÖZELÍTÉS:\n' +
             'Dunakeszi, Kolonics György utca 2/B, 2120\n' +
             'Kapucsengő: 1/43\n\n' +
             'Ha kérdésed van, hívj bátran:\n' +
             '📞 +36 30 487 7883\n\n' +
             'Várlak szeretettel az Angyali Szalonban!\n\n' +
             'Üdvözlettel,\n' +
             'Makra Edina\n' +
             'Dunakeszi Masszázs - Angyali Szalon';
  
  var handleChangeCustHtml = buildSimpleHtml('Időpont módosítva', '✅',
    '<p style="font-size:14px;color:#555;">Kedves <strong>' + data.clientName + '</strong>! Időpontod módosításra került.</p>' +
    '<div style="display:flex;gap:12px;margin:12px 0;">' +
    '<div style="flex:1;background:#fff0f0;border:1px solid #fca5a5;border-radius:6px;padding:10px;font-size:13px;"><strong>❌ Eredeti</strong><br>' + data.appointmentDate + ' ' + data.appointmentTime + '</div>' +
    '<div style="flex:1;background:#f0fdf4;border:1px solid #86efac;border-radius:6px;padding:10px;font-size:13px;"><strong>✅ Új időpont</strong><br>' + data.newDate + ' ' + data.newTime + '</div>' +
    '</div>' +
    htmlRows([['💪 Kezelés', data.service || 'Nincs megadva'], ['📍 Cím', 'Dunakeszi, Kolonics György utca 2/B'], ['🔔 Kapucsengő', '1/43']]) +
    '<p style="font-size:13px;color:#555;margin-top:8px;">Ha kérdésed van: <a href="tel:+36304877883" style="color:#D4854A;">+36 30 487 7883</a></p>'
  );
  MailApp.sendEmail({
    to: data.clientEmail,
    subject: subject,
    body: body,
    htmlBody: handleChangeCustHtml,
    name: 'Dunakeszi Masszázs - Angyali Szalon',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
  
  // Send notification to Edina
  var edinaSubject = 'Időpont módosítva - ' + data.clientName;
  var edinaBody = 'Kedves Edina!\n\n' +
                  'Egy időpont módosításra került:\n\n' +
                  '═══════════════════════════════════════\n' +
                  '👤 Név: ' + data.clientName + '\n' +
                  '📧 Email: ' + data.clientEmail + '\n' +
                  '💆 Kezelés: ' + (data.service || 'Nincs megadva') + '\n\n' +
                  '❌ EREDETI:\n' +
                  '📅 ' + data.appointmentDate + ' ' + data.appointmentTime + '\n\n' +
                  '✅ ÚJ:\n' +
                  '📅 ' + data.newDate + ' ' + data.newTime + '\n' +
                  '═══════════════════════════════════════\n\n' +
                  'Ne felejtsd el frissíteni az időpontot a Google Naptárból!';
  
  var handleChangeEdinaHtml = buildSimpleHtml('Időpont módosítva', '📋',
    '<p style="font-size:14px;color:#555;">Kedves Edina! Egy időpont módosításra került.</p>' +
    htmlRows([['👤 Név', data.clientName], ['📧 Email', data.clientEmail], ['💪 Kezelés', data.service || 'Nincs megadva']]) +
    '<div style="display:flex;gap:12px;margin:12px 0;">' +
    '<div style="flex:1;background:#fff0f0;border:1px solid #fca5a5;border-radius:6px;padding:10px;font-size:13px;"><strong>❌ Eredeti</strong><br>' + data.appointmentDate + ' ' + data.appointmentTime + '</div>' +
    '<div style="flex:1;background:#f0fdf4;border:1px solid #86efac;border-radius:6px;padding:10px;font-size:13px;"><strong>✅ Új</strong><br>' + data.newDate + ' ' + data.newTime + '</div>' +
    '</div>' +
    '<p style="font-size:13px;color:#D4854A;font-weight:bold;">⚠️ Ne felejtsd el frissíteni az időpontot a Google Naptárban!</p>'
  );
  MailApp.sendEmail({
    to: 'dunakeszimasszor@gmail.com',
    subject: edinaSubject,
    body: edinaBody,
    htmlBody: handleChangeEdinaHtml,
    name: 'Dunakeszi Masszázs - Admin',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
  
  return createResponse(true, 'Módosítási értesítés elküldve!');
}

// ============================================
// Email Functions
// ============================================
function sendBookingConfirmationToEdina(data, recurringCount, recurringType, recurringDates, discountPercent, packageId) {
  var edinaSubject = recurringCount > 1 
    ? 'Új ismétlődő foglalás - Angyali Szalon (' + data.name + ', ' + recurringCount + ' alkalom)'
    : 'Új időpontfoglalás - Angyali Szalon (' + data.name + ')';
  
  var edinaBody = 'Kedves Edina!\n\n';
  
  if (recurringCount > 1) {
    edinaBody += 'Új ISMÉTLŐDŐ foglalás érkezett a weboldalról:\n\n';
    edinaBody += '═══════════════════════════════════════\n';
    edinaBody += '👤 NÉV: ' + data.name + '\n';
    edinaBody += '📞 TELEFON: ' + (data.phone || 'Nincs megadva') + '\n';
    edinaBody += '📧 EMAIL: ' + data.email + '\n';
    edinaBody += '💆 KEZELÉS: ' + data.service + '\n';
    edinaBody += '📅 ELSŐ DÁTUM: ' + data.date + '\n';
    edinaBody += '⏰ IDŐPONT: ' + data.time + '\n';
    edinaBody += '🔄 ISMÉTLŐDÉS: ' + getRecurringTypeText(recurringType) + '\n';
    edinaBody += '📊 ALKALMAK SZÁMA: ' + recurringCount + '\n';
    if (discountPercent > 0) {
      edinaBody += '💰 KEDVEZMÉNY: ' + discountPercent + '%\n';
    }
    if (packageId) {
      edinaBody += '🎫 BÉRLET: ' + packageId + '\n';
    }
    edinaBody += '📋 ÖSSZES DÁTUM:\n';
    for (var j = 0; j < recurringDates.length; j++) {
      edinaBody += '   ' + (j + 1) + '. ' + recurringDates[j] + '\n';
    }
    edinaBody += '═══════════════════════════════════════\n\n';
  } else {
    edinaBody += 'Új időpontfoglalás érkezett a weboldalról:\n\n';
    edinaBody += '═══════════════════════════════════════\n';
    edinaBody += '👤 NÉV: ' + data.name + '\n';
    edinaBody += '📞 TELEFON: ' + (data.phone || 'Nincs megadva') + '\n';
    edinaBody += '📧 EMAIL: ' + data.email + '\n';
    edinaBody += '💆 KEZELÉS: ' + data.service + '\n';
    edinaBody += '📅 DÁTUM: ' + data.date + '\n';
    edinaBody += '⏰ IDŐPONT: ' + data.time + '\n';
    if (packageId) {
      edinaBody += '🎫 BÉRLET: ' + packageId + '\n';
    }
    edinaBody += '═══════════════════════════════════════\n\n';
  }
  
  edinaBody += '📝 MEGJEGYZÉS: ' + (data.notes || 'Nincs megadva') + '\n\n';
  edinaBody += 'A foglalás(ok) automatikusan hozzáadva lettek a Google Naptáradhoz.\n\n';
  edinaBody += 'Üdvözlettel,\n';
  edinaBody += 'Dunakeszi Masszázs Weboldal';
  
  // Build HTML version for Edina booking notification
  var edinaRows = [['👤 Név', data.name], ['📞 Telefon', data.phone || 'Nincs megadva'], ['📧 Email', data.email], ['💪 Kezelés', data.service], ['📅 ' + (recurringCount > 1 ? 'Első dátum' : 'Dátum'), data.date], ['⏰ Időpont', data.time]];
  if (recurringCount > 1) { edinaRows.push(['🔄 Ismétlődés', getRecurringTypeText(recurringType)]); edinaRows.push(['📊 Alkalmak', recurringCount + ' db']); }
  if (discountPercent > 0) edinaRows.push(['💰 Kedvezmény', discountPercent + '%']);
  if (packageId) edinaRows.push(['🎫 Bérlet', packageId]);
  edinaRows.push(['📝 Megjegyzés', data.notes || 'Nincs megadva']);
  var edinaDatesHtml = '';
  if (recurringCount > 1) {
    edinaDatesHtml = '<p style="margin:12px 0 4px;font-size:13px;color:#555;"><strong>Összes foglalt időpont:</strong></p><ol style="margin:0;padding-left:20px;font-size:13px;color:#333;">';
    for (var jh = 0; jh < recurringDates.length; jh++) { edinaDatesHtml += '<li>' + recurringDates[jh] + ' ' + data.time + '</li>'; }
    edinaDatesHtml += '</ol>';
  }
  var edinaHtmlBody = buildSimpleHtml(
    (recurringCount > 1 ? 'Új ismétlődő foglalás' : 'Új időpontfoglalás'),
    '📆',
    '<p style="font-size:14px;color:#555;">Kedves Edina! Új foglalás érkezett a weboldalról.</p>' + htmlRows(edinaRows) + edinaDatesHtml +
    '<p style="font-size:13px;color:#555;margin-top:12px;">✅ A foglalás automatikusan hozzáadva a Google Naptáradhoz.</p>'
  );
  sendEmail_({
    to: 'dunakeszimasszor@gmail.com',
    subject: edinaSubject,
    body: edinaBody,
    htmlBody: edinaHtmlBody,
    name: 'Dunakeszi Masszázs - Foglalási Rendszer',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
}

function sendBookingConfirmationToCustomer(data, recurringCount, recurringType, recurringDates, discountPercent, packageId, customer) {
  var customerSubject = recurringCount > 1
    ? 'Ismétlődő időpontfoglalás visszaigazolás - Dunakeszi Masszázs'
    : 'Időpontfoglalás visszaigazolás - Dunakeszi Masszázs';
  
  var customerBody = 'Kedves ' + data.name + '!\n\n';
  customerBody += 'Köszönöm az időpontfoglalásodat!\n\n';
  
  // Calculate price breakdown
  var servicePricePerSession = (data.service && data.service.toLowerCase().indexOf('arany') !== -1) ? 30000 : 15000;
  var totalServicePrice = servicePricePerSession * recurringCount;
  var discountAmount = discountPercent > 0 ? Math.round(totalServicePrice * discountPercent / 100) : 0;
  var discountedTotal = totalServicePrice - discountAmount;
  var depositPaid = data.amount ? Math.round(data.amount) : Math.round(discountedTotal * 0.2);
  var remainingAmount = discountedTotal - depositPaid;

  if (recurringCount > 1) {
    customerBody += '═══════════════════════════════════════\n';
    customerBody += '💆 KEZELÉS: ' + data.service + '\n';
    customerBody += '📅 ELSŐ DÁTUM: ' + data.date + '\n';
    customerBody += '⏰ IDŐPONT: ' + data.time + '\n';
    customerBody += '🔄 ISMÉTLŐDÉS: ' + getRecurringTypeText(recurringType) + '\n';
    customerBody += '📊 ALKALMAK SZÁMA: ' + recurringCount + '\n';
    customerBody += '═══════════════════════════════════════\n\n';
    customerBody += '💳 ÁRÖSSZEGZÉS:\n';
    customerBody += 'Kezelés ára (' + recurringCount + ' × ' + servicePricePerSession.toLocaleString() + ' Ft): ' + totalServicePrice.toLocaleString() + ' Ft\n';
    if (discountPercent > 0) {
      customerBody += 'Kedvezmény (' + discountPercent + '%): -' + discountAmount.toLocaleString() + ' Ft\n';
    }
    if (!packageId) {
      customerBody += 'Összesen: ' + discountedTotal.toLocaleString() + ' Ft\n';
      customerBody += '✅ Befizetett foglaló (20%): ' + depositPaid.toLocaleString() + ' Ft\n';
      customerBody += '💵 Hátralék (kezeléskor): ' + remainingAmount.toLocaleString() + ' Ft\n';
    } else {
      customerBody += '🎫 Bérletből felhasználva\n';
    }
    customerBody += '═══════════════════════════════════════\n\n';
    customerBody += '📋 AZ ÖSSZES FOGLALT IDŐPONT:\n';
    for (var k = 0; k < recurringDates.length; k++) {
      customerBody += '   ' + (k + 1) + '. ' + recurringDates[k] + ' ' + data.time + '\n';
    }
    customerBody += '\n';
  } else {
    customerBody += '═══════════════════════════════════════\n';
    customerBody += '💆 KEZELÉS: ' + data.service + '\n';
    customerBody += '📅 DÁTUM: ' + data.date + '\n';
    customerBody += '⏰ IDŐPONT: ' + data.time + '\n';
    customerBody += '═══════════════════════════════════════\n\n';
    customerBody += '💳 ÁRÖSSZEGZÉS:\n';
    customerBody += 'Kezelés ára: ' + servicePricePerSession.toLocaleString() + ' Ft\n';
    if (!packageId) {
      customerBody += '✅ Befizetett foglaló (20%): ' + depositPaid.toLocaleString() + ' Ft\n';
      customerBody += '💵 Hátralék (kezeléskor): ' + remainingAmount.toLocaleString() + ' Ft\n';
    } else {
      customerBody += '🎫 Bérletből felhasználva\n';
    }
    customerBody += '═══════════════════════════════════════\n\n';
  }
  
  // Add remaining sessions info if customer has packages
  if (customer.totalSessionsRemaining > 0) {
    customerBody += '🎫 BÉRLET EGYENLEG:\n';
    customerBody += 'Hátralévő alkalmak: ' + customer.totalSessionsRemaining + '\n\n';
  }
  
  customerBody += '📍 CÍM ÉS MEGKÖZELÍTÉS:\n';
  customerBody += 'Dunakeszi, Kolonics György utca 2/B, 2120\n';
  customerBody += 'Kapucsengő: 1/43\n\n';
  customerBody += 'A kapunál a 1/43-as kapucsengőt kell megnyomni.\n\n';
  
  if (recurringCount > 1) {
    customerBody += '⚠️ FONTOS INFORMÁCIÓ ISMÉTLŐDŐ FOGLALÁSHOZ:\n';
    customerBody += '• Minden alkalom előtt 24 órával emlékeztetőt küldök\n';
    customerBody += '• Ha bármelyik alkalomra nem tudsz eljönni, kérem, 24 órával előtte jelezd\n';
    customerBody += '• Az időpontok módosíthatók 24 óra előtt ingyenesen\n';
    if (discountPercent > 0) {
      customerBody += '• Kedvezmény: ' + discountPercent + '% a több alkalmas foglalásért\n';
    }
    customerBody += '\n';
  }
  
  customerBody += '═══════════════════════════════════════\n';
  customerBody += '🔗 FOGLALÁSAIDAT TE IS KEZELHETED ONLINE:\n';
  customerBody += '═══════════════════════════════════════\n';
  customerBody += '1️⃣  Nyisd meg: https://www.dunakeszimasszazs.hu/foglalasaim\n';
  customerBody += '2️⃣  Íd be ezt az email címet: ' + data.email + '\n';
  customerBody += '3️⃣  Megjelennek az összes közelgő foglalásaid\n';
  customerBody += '4️⃣  Minden alkalom mellé találsz ”Időpont módosítása” és ”Lemondas” gombot\n\n';
  customerBody += '⚠️ FONTOS: Lemondani vagy módosítást kérni csak 24 órával az alkalom előtt lehetkérni ingyenesen.\n\n';
  customerBody += '═══════════════════════════════════════\n\n';

  customerBody += 'Ha bármilyen kérdésed van, hívj bátran:\n';
  customerBody += '📞 +36 30 487 7883\n\n';
  customerBody += 'Várlak szeretettel az Angyali Szalonban!\n\n';
  customerBody += 'Üdvözlettel,\n';
  customerBody += 'Makra Edina\n';
  customerBody += 'Dunakeszi Masszázs - Angyali Szalon\n';
  customerBody += '📞 +36 30 487 7883\n';
  customerBody += '📧 dunakeszimasszor@gmail.com\n';
  customerBody += '🌐 https://dunakeszimasszazs.hu';
  
  sendEmail_({
    to: data.email,
    subject: customerSubject,
    body: customerBody,
    htmlBody: buildCustomerConfirmationHtml(data, recurringCount, recurringType, recurringDates, discountPercent, packageId, customer),
    name: 'Dunakeszi Masszázs - Angyali Szalon',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
}

function sendPackagePurchaseConfirmation(customer, pkg) {
  var subject = 'Bérlet vásárlás visszaigazolás - Dunakeszi Masszázs';
  
  var body = 'Kedves ' + customer.name + '!\n\n';
  body += 'Köszönöm a bérlet vásárlásodat!\n\n';
  body += '═══════════════════════════════════════\n';
  body += '🎫 BÉRLET RÉSZLETEI:\n';
  body += 'Bérlet száma: ' + pkg.packageId + '\n';
  body += 'Alkalmak száma: ' + pkg.sessions + '\n';
  body += 'Eredeti ár: ' + pkg.originalPrice.toLocaleString() + ' Ft\n';
  if (pkg.discountPercent > 0) {
    body += 'Kedvezmény: ' + pkg.discountPercent + '% (-' + pkg.discountAmount.toLocaleString() + ' Ft)\n';
  }
  body += 'Fizetendő: ' + pkg.finalPrice.toLocaleString() + ' Ft\n';
  body += 'Foglaló befizetve: ' + pkg.depositPaid.toLocaleString() + ' Ft\n';
  body += 'Hátralék: ' + (pkg.finalPrice - pkg.depositPaid).toLocaleString() + ' Ft\n';
  body += '═══════════════════════════════════════\n\n';
  
  body += 'Hogyan használhatod a bérletet:\n';
  body += '• Foglalj időpontot a weboldalon vagy telefonon\n';
  body += '• A rendszer automatikusan levon egy alkalmat a bérletedből\n';
  body += '• A hátralékot az első kezelésnél kell kifizetni\n\n';
  
  body += 'Bérlet egyenleged: ' + (customer.totalSessionsRemaining + pkg.sessions) + ' alkalom\n\n';
  
  body += 'Ha kérdésed van, hívj bátran:\n';
  body += '📞 +36 30 487 7883\n\n';
  body += 'Üdvözlettel,\n';
  body += 'Makra Edina\n';
  body += 'Dunakeszi Masszázs - Angyali Szalon';
  
  var pkgRows = [['🎫 Bérlet száma', pkg.packageId], ['📅 Alkalmak', pkg.sessions + ' db'], ['💰 Eredeti ár', pkg.originalPrice.toLocaleString() + ' Ft']];
  if (pkg.discountPercent > 0) pkgRows.push(['🎉 Kedvezmény', pkg.discountPercent + '% (−' + pkg.discountAmount.toLocaleString() + ' Ft)']);
  pkgRows.push(['✅ Befizetett foglaló', pkg.depositPaid.toLocaleString() + ' Ft']);
  pkgRows.push(['💵 Hátralék', (pkg.finalPrice - pkg.depositPaid).toLocaleString() + ' Ft']);
  var pkgHtml = buildSimpleHtml('Bérlet vásárlás visszaigazolás', '🎫',
    '<p style="font-size:14px;color:#555;">Kedves <strong>' + customer.name + '</strong>! Köszönöm a bérlet vásárlásodat.</p>' +
    htmlRows(pkgRows) +
    '<div style="background:#fff8f0;border:1px solid #fcd9b0;border-radius:6px;padding:12px;margin-top:12px;font-size:13px;color:#555;">' +
    '<strong>Hogyan használd a bérleted?</strong><ul style="margin:8px 0;padding-left:18px;">' +
    '<li>Foglalj időpontot a weboldalon vagy telefonon</li>' +
    '<li>A rendszer automatikusan levon egy alkalmat</li>' +
    '<li>A hátralékot az első kezelésnél kell kifizetni</li>' +
    '</ul></div>'
  );
  sendEmail_({
    to: customer.email,
    subject: subject,
    body: body,
    htmlBody: pkgHtml,
    name: 'Dunakeszi Masszázs - Angyali Szalon',
    replyTo: 'dunakeszimasszor@gmail.com'
  });
}

// ============================================
// Stripe Webhook Handler
// ============================================
// Configure in Stripe Dashboard → Developers → Webhooks → Add endpoint
// Endpoint URL: your Google Apps Script web app URL
// Events to listen for: checkout.session.completed
function handleStripeWebhook(event) {
  try {
    Logger.log('Stripe webhook received: ' + event.type);

    if (event.type === 'checkout.session.completed') {
      var session = event.data.object;
      var metadata = session.metadata || {};

      // Build booking data from Stripe session metadata
      var bookingData = {
        name: metadata.customer_name || '',
        email: metadata.customer_email || session.customer_email || '',
        phone: metadata.customer_phone || '',
        service: metadata.service || '',
        date: metadata.date || '',
        time: metadata.time || '',
        notes: metadata.notes || '',
        recurring: metadata.recurring || 'no',
        recurringType: metadata.recurring_type || 'weekly',
        recurringCount: metadata.recurring_count || '1',
        usePackage: 'false',
        stripePaymentIntentId: session.payment_intent || ''
      };

      Logger.log('Creating booking from webhook: ' + JSON.stringify(bookingData));

      // Create the actual booking: calendar event, spreadsheet record, emails
      createBooking(bookingData);

      // Mark the pending booking row as paid
      updatePendingBookingStatus(bookingData.email, 'paid');

      // Record the payment in the Financial sheet
      recordTransaction(
        new Date(),
        'Payment',
        '',
        bookingData.name,
        'Stripe foglalási díj - ' + bookingData.service,
        Math.round(session.amount_total / 100), // fillér → Ft
        'Stripe',
        session.id
      );
    }

    // Stripe expects a 200 response
    return createResponse(true, 'Webhook feldolgozva: ' + event.type);
  } catch (error) {
    logError('handleStripeWebhook', error);
    return createResponse(false, 'Webhook hiba: ' + error.message);
  }
}

function updatePendingBookingStatus(email, status) {
  var pendingSheet = ensurePendingBookingsSheet_();
  if (!pendingSheet) return;

  var data = pendingSheet.getDataRange().getValues();
  // Search from the bottom so we update the most recent pending entry
  for (var i = data.length - 1; i >= 1; i--) {
    if (data[i][2] === email && (data[i][12] === 'pending' || data[i][12] === 'awaiting_bank_transfer')) {
      pendingSheet.getRange(i + 1, 13).setValue(status);
      Logger.log('Updated pending booking for ' + email + ' to ' + status);
      break;
    }
  }
}

// ============================================
// Availability Check, Error Logging & HTML Email
// ============================================
function isTimeSlotAvailable(dateStr, timeStr) {
  var calendar = CalendarApp.getDefaultCalendar();
  var start = new Date(dateStr + 'T' + timeStr + ':00');
  // 15-min buffer before + 75-min after (60-min massage + 15-min cleanup)
  var searchStart = new Date(start.getTime() - (15 * 60 * 1000));
  var searchEnd   = new Date(start.getTime() + (75 * 60 * 1000));
  var events = calendar.getEvents(searchStart, searchEnd);
  return events.length === 0;
}

// Returns availability of all time slots for a given date
function getSlotsForDate_(dateStr) {
  if (!dateStr) return createResponse(false, 'Hiányzó dátum');
  var slots = ['08:30', '09:45', '11:00', '12:15', '13:30', '14:45', '16:00', '17:15', '18:30'];
  var result = {};
  for (var i = 0; i < slots.length; i++) {
    result[slots[i]] = isTimeSlotAvailable(dateStr, slots[i]);
  }
  return createResponse(true, 'OK', { slots: result });
}

// Block a time slot by creating a calendar event (admin use)
// data: { date, time, label (optional), duration (optional minutes, default 75) }
function blockSlot_(data) {
  if (!data.date || !data.time) return createResponse(false, 'Hiányzó dátum vagy időpont');
  try {
    var calendar = CalendarApp.getDefaultCalendar();
    var dateParts = data.date.split('-');
    var timeParts = data.time.split(':');
    var start = new Date(
      parseInt(dateParts[0]), parseInt(dateParts[1]) - 1, parseInt(dateParts[2]),
      parseInt(timeParts[0]), parseInt(timeParts[1])
    );
    var duration = parseInt(data.duration) || 75;
    var end = new Date(start.getTime() + duration * 60 * 1000);
    var title = data.label ? '🔒 ' + data.label : '🔒 Zárolt időpont';
    var event = calendar.createEvent(title, start, end, {
      description: 'Admin által zárolt időpont. Nem foglalható.'
    });
    event.setColor('8'); // graphite / dark
    return createResponse(true, 'Időpont sikeresen zárolva: ' + data.date + ' ' + data.time, { eventId: event.getId() });
  } catch(e) {
    return createResponse(false, 'Hiba a zárolás során: ' + e.message);
  }
}

// Email wrapper — skips actual sending when TEST_MODE is active
// This prevents hitting the daily MailApp quota during automated test runs.
function sendEmail_(opts) {
  var props = PropertiesService.getScriptProperties();
  if (props.getProperty('TEST_MODE') === 'true') {
    Logger.log('[TEST_MODE] Email suppressed. To: ' + opts.to + ' | Subject: ' + opts.subject);
    return;
  }
  MailApp.sendEmail(opts);
}

function logError(context, error) {
  try {
    var ss = SpreadsheetApp.getActiveSpreadsheet();
    var sheet = ss.getSheetByName(SHEETS.LOGS);
    if (!sheet) {
      sheet = ss.insertSheet(SHEETS.LOGS);
      sheet.appendRow(['Időpont', 'Helyszín', 'Hibaüzenet']);
      sheet.getRange(1, 1, 1, 3).setFontWeight('bold');
    }
    sheet.appendRow([new Date(), context, error.toString()]);
  } catch (logErr) {
    Logger.log('logError failed: ' + logErr);
  }
  Logger.log('[' + context + '] ' + error);
}

function buildCustomerConfirmationHtml(data, recurringCount, recurringType, recurringDates, discountPercent, packageId, customer) {
  var primaryColor = '#D4854A';
  var html = '<div style="font-family:Arial,sans-serif;max-width:600px;margin:0 auto;border:1px solid #e0d6c8;border-radius:12px;overflow:hidden;">';

  // Price calculations
  var servicePricePerSession = (data.service && data.service.toLowerCase().indexOf('arany') !== -1) ? 30000 : 15000;
  var totalServicePrice = servicePricePerSession * recurringCount;
  var discountAmount = discountPercent > 0 ? Math.round(totalServicePrice * discountPercent / 100) : 0;
  var discountedTotal = totalServicePrice - discountAmount;
  var depositPaid = data.amount ? Math.round(data.amount) : Math.round(discountedTotal * 0.2);
  var remainingAmount = discountedTotal - depositPaid;

  // Header
  html += '<div style="background:' + primaryColor + ';padding:24px;text-align:center;">';
  html += '<h1 style="color:white;margin:0;font-size:24px;">&#10003; Sikeres Foglalás!</h1>';
  html += '<p style="color:rgba(255,255,255,0.9);margin:8px 0 0;">Dunakeszi Masszázs - Angyali Szalon</p>';
  html += '</div>';

  // Body
  html += '<div style="padding:24px;background:#fdfaf7;">';
  html += '<p>Kedves <b>' + data.name + '</b>!</p>';
  html += '<p>Köszönöm az időpontfoglalásodat! Az alábbiakban találod a foglalásod részleteit:</p>';

  // Details box
  html += '<div style="background:white;border:1px solid #e0d6c8;border-radius:8px;padding:16px;margin:16px 0;">';
  html += '<p style="margin:6px 0;">&#128134; <b>Kezelés:</b> ' + data.service + '</p>';

  if (recurringCount > 1) {
    html += '<p style="margin:6px 0;">&#128197; <b>Első dátum:</b> ' + data.date + '</p>';
    html += '<p style="margin:6px 0;">&#8987; <b>Időpont:</b> ' + data.time + '</p>';
    html += '<p style="margin:6px 0;">&#128260; <b>Ismétlődés:</b> ' + getRecurringTypeText(recurringType) + '</p>';
    html += '<p style="margin:6px 0;">&#128202; <b>Alkalmak száma:</b> ' + recurringCount + '</p>';
    html += '<hr style="border:none;border-top:1px solid #e0d6c8;margin:12px 0;">';
    html += '<p style="margin:6px 0;"><b>&#128203; Összes foglalt időpont:</b></p>';
    for (var k = 0; k < recurringDates.length; k++) {
      html += '<p style="margin:4px 0 4px 16px;">' + (k + 1) + '. ' + recurringDates[k] + ' ' + data.time + '</p>';
    }
  } else {
    html += '<p style="margin:6px 0;">&#128197; <b>Dátum:</b> ' + data.date + '</p>';
    html += '<p style="margin:6px 0;">&#8987; <b>Időpont:</b> ' + data.time + '</p>';
  }
  html += '</div>';

  // Price breakdown box
  html += '<div style="background:#fff8f0;border:1px solid #f0dcc8;border-radius:8px;padding:16px;margin:16px 0;">';
  html += '<p style="margin:0 0 10px;font-weight:bold;color:#4A3F35;">&#128176; Árösszegzés</p>';
  if (recurringCount > 1) {
    html += '<div style="display:flex;justify-content:space-between;margin:4px 0;font-size:14px;">';
    html += '<span style="color:#8B7355;">Kezelés ára (' + recurringCount + ' × ' + servicePricePerSession.toLocaleString() + ' Ft):</span>';
    html += '<span style="color:#4A3F35;font-weight:500;">' + totalServicePrice.toLocaleString() + ' Ft</span></div>';
  } else {
    html += '<div style="display:flex;justify-content:space-between;margin:4px 0;font-size:14px;">';
    html += '<span style="color:#8B7355;">Kezelés ára:</span>';
    html += '<span style="color:#4A3F35;font-weight:500;">' + servicePricePerSession.toLocaleString() + ' Ft</span></div>';
  }
  if (discountPercent > 0) {
    html += '<div style="display:flex;justify-content:space-between;margin:4px 0;font-size:14px;">';
    html += '<span style="color:#8B9A7C;">Kedvezmény (' + discountPercent + '%):</span>';
    html += '<span style="color:#8B9A7C;">-' + discountAmount.toLocaleString() + ' Ft</span></div>';
    html += '<div style="display:flex;justify-content:space-between;margin:4px 0 8px;font-size:14px;border-top:1px solid #e0d6c8;padding-top:8px;">';
    html += '<span style="color:#4A3F35;font-weight:bold;">Összesen:</span>';
    html += '<span style="color:#4A3F35;font-weight:bold;">' + discountedTotal.toLocaleString() + ' Ft</span></div>';
  }
  if (!packageId) {
    html += '<div style="display:flex;justify-content:space-between;margin:4px 0;font-size:14px;background:#e8f5e9;padding:6px 8px;border-radius:4px;">';
    html += '<span style="color:#2e7d32;">&#10003; Befizetett foglaló (20%):</span>';
    html += '<span style="color:#2e7d32;font-weight:bold;">' + depositPaid.toLocaleString() + ' Ft</span></div>';
    html += '<div style="display:flex;justify-content:space-between;margin:4px 0;font-size:14px;background:#fff3e0;padding:6px 8px;border-radius:4px;">';
    html += '<span style="color:#e65100;">&#128197; Hátralék (kezeléskor fizetendő):</span>';
    html += '<span style="color:#e65100;font-weight:bold;">' + remainingAmount.toLocaleString() + ' Ft</span></div>';
  } else {
    html += '<div style="margin:4px 0;font-size:14px;color:#4A7C59;font-weight:500;">&#127903; Bérletből felhasználva</div>';
  }
  html += '</div>';

  if (customer && customer.totalSessionsRemaining > 0) {
    html += '<div style="background:#e8f5e9;border-radius:8px;padding:12px;margin:12px 0;">';
    html += '<p style="margin:0;">&#127903; <b>Bérlet egyenleg:</b> ' + customer.totalSessionsRemaining + ' alkalom</p>';
    html += '</div>';
  }

  // Address
  html += '<div style="background:#fff3e0;border-radius:8px;padding:12px;margin:12px 0;">';
  html += '<p style="margin:0 0 6px;"><b>&#128205; Cím és megközelítés:</b></p>';
  html += '<p style="margin:4px 0;">Dunakeszi, Kolonics György utca 2/B, 2120</p>';
  html += '<p style="margin:4px 0;">Kapucsengő: <b>1/43</b></p>';
  html += '</div>';

  html += '<div style="background:#EFF6FF;border:1px solid #BFDBFE;border-radius:8px;padding:16px;margin:16px 0;">';
  html += '<p style="margin:0 0 10px;font-size:14px;color:#1D4ED8;font-weight:bold;">&#128197; Szeretned módosítani vagy lemondani egy időpontot?</p>';
  html += '<p style="margin:0 0 10px;font-size:13px;color:#374151;">Ezt könnyen meg tudod tenni online, bármikor:</p>';
  html += '<ol style="margin:0 0 12px;padding-left:20px;font-size:13px;color:#374151;line-height:2;">';
  html += '<li>Kattints az alábbi gombra</li>';
  html += '<li>Íd be <b>' + data.email + '</b> címed</li>';
  html += '<li>Megjelennek az összes közelgő foglalásaid</li>';
  html += '<li>Minden alkalom mellé találsz <b>Módosítás</b> és <b>Lemondás</b> gombot</li>';
  html += '</ol>';
  html += '<div style="text-align:center;margin:12px 0 8px;">';
  html += '<a href="https://www.dunakeszimasszazs.hu/foglalasaim" style="display:inline-block;background:#D4854A;color:white;text-decoration:none;padding:12px 28px;border-radius:20px;font-size:14px;font-weight:bold;">Foglalásaim kezelése &rarr;</a>';
  html += '</div>';
  html += '<p style="margin:8px 0 0;font-size:11px;color:#6B7280;text-align:center;">&#9888;&#65039; Ingyenes lemondas / módosítás kérés 24 órával az alkalom előtt lehetkérni</p>';
  html += '</div>';

  html += '<p style="font-size:13px;color:#666;">Ha kérdésed van, hívj bátran: <b>+36 30 487 7883</b></p>';
  html += '<p style="font-size:13px;color:#666;">Várlak szeretettel az Angyali Szalonban!</p>';
  html += '</div>';

  // Footer strip
  html += '<div style="background:#f5ede3;padding:12px;text-align:center;font-size:12px;color:#888;">';
  html += 'Makra Edina &middot; Dunakeszi Masszázs &middot; <a href="https://dunakeszimasszazs.hu" style="color:#D4854A;">dunakeszimasszazs.hu</a>';
  html += '</div>';

  html += '</div>';
  return html;
}

// ============================================
// Helper Functions
// ============================================
function createResponse(success, message, data) {
  var response = { success: success, message: message };
  if (data) { response.data = data; }
  
  var output = ContentService.createTextOutput(JSON.stringify(response));
  output.setMimeType(ContentService.MimeType.JSON);
  return output;
}

function createHtmlResponse(html) {
  var output = ContentService.createTextOutput(html);
  output.setMimeType(ContentService.MimeType.HTML);
  return output;
}

// Timezone for all user-facing date/time formatting
var TZ = 'Europe/Budapest';

// In-memory set of booking keys created in THIS script execution.
// GAS caches sheet reads within a single execution — appendRow+flush() does NOT
// invalidate the read cache, so dupData reads would return stale (pre-write) data.
// This set is the ground truth for same-execution duplicate detection.
// Key format: "customerId|YYYY-MM-DD|HH:MM"
var _createdBookingKeys = {};

// Format a JS Date as YYYY-MM-DD in Budapest time
function formatDate(date) {
  return Utilities.formatDate(date, TZ, 'yyyy-MM-dd');
}

// Safely extract HH:MM from a sheet cell value.
// Google Sheets stores time-typed cells as Date objects (Dec 30 1899 HH:MM:SS).
// This handles both that case and plain strings like "09:45" or "09:45:00".
function formatTimeFromSheet(val) {
  if (!val && val !== 0) return '';
  if (val instanceof Date) {
    return String(val.getHours()).padStart(2, '0') + ':' + String(val.getMinutes()).padStart(2, '0');
  }
  // String — take first 5 chars ("HH:MM")
  return String(val).slice(0, 5);
}

// ============================================
// Generic branded HTML email builder
// Wraps any HTML content in the salon's orange-branded card.
// Used for ALL emails except the rich customer booking confirmation
// (which has its own buildCustomerConfirmationHtml).
// ============================================
function buildSimpleHtml(title, icon, contentHtml) {
  var pc = '#D4854A'; // primary orange
  var h = '<!DOCTYPE html><html><body style="margin:0;padding:0;background:#f5f0eb;font-family:Arial,sans-serif;">';
  h += '<div style="max-width:600px;margin:24px auto;border:1px solid #e0d6c8;border-radius:12px;overflow:hidden;">';
  // Header
  h += '<div style="background:' + pc + ';padding:20px 24px;text-align:center;">';
  h += '<div style="font-size:32px;margin-bottom:6px;">' + icon + '</div>';
  h += '<h1 style="color:#fff;margin:0;font-size:20px;font-weight:bold;">' + title + '</h1>';
  h += '<p style="color:rgba(255,255,255,0.85);margin:4px 0 0;font-size:13px;">Dunakeszi Masszázs – Angyali Szalon</p>';
  h += '</div>';
  // Body
  h += '<div style="padding:24px;background:#fdfaf7;">' + contentHtml + '</div>';
  // Footer
  h += '<div style="background:#f0ece6;padding:16px 24px;text-align:center;border-top:1px solid #e0d6c8;">';
  h += '<p style="margin:0;font-size:12px;color:#888;">Makra Edina · Dunakeszi Masszázs – Angyali Szalon</p>';
  h += '<p style="margin:4px 0 0;font-size:12px;color:#888;">📞 +36 30 487 7883 · <a href="https://dunakeszimasszazs.hu" style="color:' + pc + ';">dunakeszimasszazs.hu</a></p>';
  h += '</div>';
  h += '</div></body></html>';
  return h;
}

// Renders a detail-row table for buildSimpleHtml
function htmlRows(rows) {
  var s = '<table style="width:100%;border-collapse:collapse;margin:16px 0;">';
  for (var i = 0; i < rows.length; i++) {
    var bg = i % 2 === 0 ? '#fff' : '#faf6f2';
    s += '<tr style="background:' + bg + ';">';
    s += '<td style="padding:8px 12px;font-size:13px;color:#666;width:45%;border-bottom:1px solid #ede8e2;">' + rows[i][0] + '</td>';
    s += '<td style="padding:8px 12px;font-size:13px;color:#333;font-weight:bold;border-bottom:1px solid #ede8e2;">' + rows[i][1] + '</td>';
    s += '</tr>';
  }
  s += '</table>';
  return s;
}

function getRecurringTypeText(type) {
  switch(type) {
    case 'weekly': return 'Minden héten';
    case 'biweekly': return 'Kéthetente';
    case 'monthly': return 'Minden hónapban';
    default: return type;
  }
}

function calculateDiscount(sessionCount) {
  if (sessionCount >= 6) return 15;
  if (sessionCount >= 4) return 10;
  if (sessionCount >= 2) return 5;
  return 0;
}

// ============================================
// SYSTEM TEST RUNNER
// Run manually from the Apps Script editor (NOT deployed).
// Tests every backend feature. Creates [TEST] tagged data,
// runs all checks, then cleans itself up automatically.
// NOTE: Stripe checkout is intentionally NOT tested here
//       (live keys are active – never test Stripe from GAS directly).
// Test notification emails will be sent to Edina with [TEST] in the name.
// ============================================
function runSystemTests() {
  // Suppress emails during test run to avoid hitting daily MailApp quota
  PropertiesService.getScriptProperties().setProperty('TEST_MODE', 'true');

  var TAG = '[TEST]';
  var testEmail = 'dunakeszimasszor@gmail.com'; // test emails land here
  var testName  = TAG + ' Auto Teszt';
  var testPhone = '+36 99 000 0000';
  var testService = 'Frissítő masszázs';

  // Dates: 3 and 4 days from now (safely outside 24h restriction)
  var d3 = new Date(); d3.setDate(d3.getDate() + 3);
  var d4 = new Date(); d4.setDate(d4.getDate() + 4);
  var dateA = formatDate(d3); // used for single + duplicate tests
  var dateB = formatDate(d4); // used for recurring test
  var timeA = '10:00';
  var timeB = '11:15';
  var timeC = '14:00'; // used for change-request target

  // Date within 24h for policy enforcement test (2h from now)
  var dSoon = new Date(); dSoon.setHours(dSoon.getHours() + 2);
  var date24h = formatDate(dSoon);
  var time24h = String(dSoon.getHours()).padStart(2,'0') + ':' + String(dSoon.getMinutes()).padStart(2,'0');

  var passed = 0, failed = 0, skipped = 0;
  var log = [];

  function pass(name)          { passed++;  log.push('✅ PASS: ' + name);  Logger.log('PASS: ' + name); }
  function fail(name, detail)  { failed++;  log.push('❌ FAIL: ' + name + '\n       → ' + detail); Logger.log('FAIL: ' + name + ' – ' + detail); }
  function skip(name, reason)  { skipped++; log.push('⏭️  SKIP: ' + name + ' (' + reason + ')'); }

  // Safely parse response — functions return plain objects via createResponse()
  function r(resp) {
    if (!resp) return { success: false, message: 'null response' };
    if (typeof resp === 'object' && 'success' in resp) return resp;
    try { return JSON.parse(resp.getContent ? resp.getContent() : resp); } catch(e) { return { success: false, message: 'parse error: ' + e }; }
  }

  log.push('╔══════════════════════════════════════════╗');
  log.push('║  DUNAKESZI MASSZÁZS – RENDSZERTESZT      ║');
  log.push('╚══════════════════════════════════════════╝');
  log.push('Futtatva: ' + new Date().toLocaleString('hu-HU'));
  log.push('Teszt dátum A: ' + dateA + ' ' + timeA);
  log.push('Teszt dátum B: ' + dateB + ' ' + timeA + ' (ismétlődő)');
  log.push('24h teszt: ' + date24h + ' ' + time24h);
  log.push('');

  // ── 1. Vásárló létrehozás ──────────────────────────
  var testCustomerId;
  try {
    var c1 = getOrCreateCustomer(testName, testEmail, testPhone);
    if (c1 && c1.customerId) { testCustomerId = c1.customerId; pass('1. Vásárló létrehozás'); }
    else { fail('1. Vásárló létrehozás', JSON.stringify(c1)); }
  } catch(e) { fail('1. Vásárló létrehozás', e.toString()); }

  // ── 2. Duplikált vásárló megelőzés ────────────────
  try {
    var c2 = getOrCreateCustomer(testName, testEmail, testPhone);
    if (c2 && c2.customerId === testCustomerId) { pass('2. Duplikált vásárló megelőzés (ugyanaz az ID)'); }
    else { fail('2. Duplikált vásárló megelőzés', 'Különböző ID: ' + (c2 ? c2.customerId : 'null')); }
  } catch(e) { fail('2. Duplikált vásárló megelőzés', e.toString()); }

  // ── 3. Egyszeri foglalás létrehozás ───────────────
  try {
    var r3 = r(createBooking({ name: testName, email: testEmail, phone: testPhone,
      service: testService, date: dateA, time: timeA, recurring: 'no', recurringCount: '1', notes: TAG }));
    if (r3.success) { pass('3. Egyszeri foglalás létrehozás'); }
    else { fail('3. Egyszeri foglalás létrehozás', r3.message); }
  } catch(e) { fail('3. Egyszeri foglalás létrehozás', e.toString()); }

  // ── 4. Duplikált időpont megelőzés ────────────────
  try {
    var r4 = r(createBooking({ name: testName, email: testEmail, phone: testPhone,
      service: testService, date: dateA, time: timeA, recurring: 'no', recurringCount: '1', notes: TAG }));
    if (!r4.success && r4.message && r4.message.indexOf('már van') !== -1) { pass('4. Duplikált időpont megelőzés'); }
    else { fail('4. Duplikált időpont megelőzés', 'El kellett volna utasítani. Kapott: ' + JSON.stringify(r4)); }
  } catch(e) { fail('4. Duplikált időpont megelőzés', e.toString()); }

  // ── 5. Ismétlődő foglalás + automatikus bérlet ────
  try {
    var r5 = r(createBooking({ name: testName, email: testEmail, phone: testPhone,
      service: testService, date: dateB, time: timeA, recurring: 'yes', recurringCount: '3', recurringType: 'weekly', notes: TAG }));
    if (r5.success) {
      var pkgs5 = r(getAllPackages_());
      var myPkgs = (pkgs5.data && pkgs5.data.packages ? pkgs5.data.packages : [])
        .filter(function(p) { return p.customerName === testName && Number(p.sessionsPurchased) === 3; });
      if (myPkgs.length > 0) { pass('5. Ismétlődő foglalás (3 alkalom) + automatikus bérlet létrehozás'); }
      else { fail('5. Automatikus bérlet létrehozás', 'Bérlet nem jelent meg a SessionPackages-ben'); }
    } else { fail('5. Ismétlődő foglalás létrehozás', r5.message); }
  } catch(e) { fail('5. Ismétlődő foglalás + automatikus bérlet', e.toString()); }

  // ── 6. Saját foglalások lekérése (dátum formátum) ─
  var myBookingsData;
  try {
    var r6 = r(getMyBookings_(testEmail));
    if (r6.success && r6.data && r6.data.bookings) {
      myBookingsData = r6.data.bookings;
      var allDates = myBookingsData.every(function(b) { return /^\d{4}-\d{2}-\d{2}$/.test(b.date); });
      var allTimes = myBookingsData.every(function(b) { return /^\d{2}:\d{2}$/.test(b.time); });
      if (myBookingsData.length > 0 && allDates && allTimes) { pass('6. Saját foglalások (helyes YYYY-MM-DD dátum, HH:MM idő – ' + myBookingsData.length + ' db)'); }
      else { fail('6. Saját foglalások formátum', 'dates ok=' + allDates + ' times ok=' + allTimes + ' count=' + myBookingsData.length); }
    } else { fail('6. Saját foglalások lekérése', r6.message); }
  } catch(e) { fail('6. Saját foglalások lekérése', e.toString()); }

  // ── 7. Összes foglalás lekérése (admin) ───────────
  try {
    var r7 = r(getAllBookings_());
    if (r7.success && r7.data && r7.data.bookings) {
      var mine7 = r7.data.bookings.filter(function(b) { return b.customerName === testName; });
      var datesOk = mine7.every(function(b) { return /^\d{4}-\d{2}-\d{2}$/.test(b.date); });
      var timesOk = mine7.every(function(b) { return /^\d{2}:\d{2}$/.test(b.time); });
      var createdOk = mine7.every(function(b) { return b.createdDate && b.createdDate.length >= 8; });
      if (mine7.length > 0 && datesOk && timesOk && createdOk) { pass('7. Összes foglalás (helyes dátum/idő/létrehozva formátum – ' + mine7.length + ' db)'); }
      else { fail('7. Összes foglalás formátum', 'dates=' + datesOk + ' times=' + timesOk + ' created=' + createdOk + ' count=' + mine7.length); }
    } else { fail('7. Összes foglalás lekérése', r7.message); }
  } catch(e) { fail('7. Összes foglalás lekérése', e.toString()); }

  // ── 8. Bérletek lekérése ──────────────────────────
  try {
    var r8 = r(getAllPackages_());
    if (r8.success && r8.data && r8.data.packages) {
      var myPkgs8 = r8.data.packages.filter(function(p) { return p.customerName === testName; });
      if (myPkgs8.length > 0) { pass('8. Bérletek lekérése (' + myPkgs8.length + ' teszt bérlet)'); }
      else { fail('8. Bérletek lekérése', 'Nincs bérlet a teszt vásárló nevén'); }
    } else { fail('8. Bérletek lekérése', r8.message); }
  } catch(e) { fail('8. Bérletek lekérése', e.toString()); }

  // ── 9. Saját lemondás ─────────────────────────────
  var confirmedBefore9 = myBookingsData ? myBookingsData.filter(function(b) { return b.status === 'Confirmed'; }) : [];
  if (confirmedBefore9.length > 0) {
    try {
      var r9 = r(selfCancelBooking_({ bookingId: confirmedBefore9[0].bookingId, email: testEmail, reason: 'Automatikus teszt' }));
      if (r9.success) { pass('9. Saját foglalás lemondás'); }
      else { fail('9. Saját foglalás lemondás', r9.message); }
    } catch(e) { fail('9. Saját foglalás lemondás', e.toString()); }
  } else { skip('9. Saját foglalás lemondás', 'Nincs Confirmed foglalás'); }

  // ── 10. 24 órás szabály érvényesítés ─────────────
  try {
    var r10c = r(createBooking({ name: testName, email: testEmail, phone: testPhone,
      service: testService, date: date24h, time: time24h, recurring: 'no', recurringCount: '1', notes: TAG + '_24H' }));
    if (r10c.success) {
      // Use the bookingId returned directly — avoids stale sheet read cache
      var bid10 = (r10c.data && r10c.data.bookingIds && r10c.data.bookingIds.length > 0) ? r10c.data.bookingIds[0] : null;
      if (bid10) {
        var r10 = r(selfCancelBooking_({ bookingId: bid10, email: testEmail, reason: 'teszt' }));
        if (!r10.success && r10.message && r10.message.indexOf('24') !== -1) { pass('10. 24 órás lemondási szabály érvényesítés'); }
        else { fail('10. 24 órás lemondási szabály', 'El kellett volna utasítani 24h üzenettel. Kapott: ' + JSON.stringify(r10)); }
      } else { skip('10. 24 órás lemondási szabály', '24h-s bookingId nem érhető el a válaszban'); }
    } else { skip('10. 24 órás lemondási szabály', 'Nem sikerült közeli foglalást létrehozni: ' + r10c.message); }
  } catch(e) { fail('10. 24 órás lemondási szabály', e.toString()); }

  // Force-cancel any near-term (<25h) [TEST] booking so it doesn't become confirmed11[0]
  try {
    var bSh10 = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEETS.BOOKINGS);
    var bD10 = bSh10.getDataRange().getValues();
    for (var bi10 = bD10.length - 1; bi10 >= 1; bi10--) {
      if (String(bD10[bi10][2]).indexOf(TAG) !== -1 && bD10[bi10][6] === 'Confirmed') {
        var rowDate10 = typeof bD10[bi10][4] === 'string' ? bD10[bi10][4].slice(0,10) : formatDate(new Date(bD10[bi10][4]));
        var rowTime10 = formatTimeFromSheet(bD10[bi10][5]);
        var rowMs10 = new Date(rowDate10 + 'T' + rowTime10 + ':00').getTime();
        if ((rowMs10 - Date.now()) < 25 * 60 * 60 * 1000) {
          bSh10.getRange(bi10 + 1, 7).setValue('Cancelled');
        }
      }
    }
    SpreadsheetApp.flush();
  } catch(e10c) { Logger.log('Near-term test cleanup skipped: ' + e10c); }

  // ── 11. Módosítási kérelem ────────────────────────
  var freshBookings11;
  try {
    var mb11 = r(getMyBookings_(testEmail));
    freshBookings11 = mb11.data && mb11.data.bookings ? mb11.data.bookings : [];
    // Only pick a booking safely >25h in the future to avoid 24h policy rejection
    var now11 = Date.now();
    var confirmed11 = freshBookings11.filter(function(b) {
      return b.status === 'Confirmed' &&
             (new Date(b.date + 'T' + b.time + ':00').getTime() - now11) > 25 * 60 * 60 * 1000;
    });
    if (confirmed11.length > 0) {
      var d5 = new Date(); d5.setDate(d5.getDate() + 5);
      var r11 = r(selfRequestChange_({ bookingId: confirmed11[0].bookingId, email: testEmail, newDate: formatDate(d5), newTime: timeC, notes: 'teszt módosítás' }));
      if (r11.success) { pass('11. Módosítási kérelem küldés + ChangeRequested státusz'); }
      else { fail('11. Módosítási kérelem', r11.message); }
    } else { skip('11. Módosítási kérelem', 'Nincs >25h jövőbeli Confirmed foglalás'); }
  } catch(e) { fail('11. Módosítási kérelem', e.toString()); }

  // ── 12. Ütköző módosítás megelőzés ───────────────
  try {
    var mb12 = r(getMyBookings_(testEmail));
    var all12 = mb12.data && mb12.data.bookings ? mb12.data.bookings : [];
    var now12 = Date.now();
    var conf12 = all12.filter(function(b) {
      return b.status === 'Confirmed' &&
             (new Date(b.date + 'T' + b.time + ':00').getTime() - now12) > 25 * 60 * 60 * 1000;
    });
    var others12 = all12.filter(function(b) {
      return (b.status === 'Confirmed' || b.status === 'ChangeRequested') &&
             (new Date(b.date + 'T' + b.time + ':00').getTime() - now12) > 25 * 60 * 60 * 1000;
    });
    var target12 = others12.find(function(b) { return conf12.length > 0 && b.bookingId !== conf12[0].bookingId; });
    if (conf12.length > 0 && target12) {
      var r12 = r(selfRequestChange_({ bookingId: conf12[0].bookingId, email: testEmail, newDate: target12.date, newTime: target12.time, notes: 'ütközés teszt' }));
      if (!r12.success && r12.message && r12.message.indexOf('már van') !== -1) { pass('12. Ütköző módosítás megelőzés'); }
      else { fail('12. Ütköző módosítás megelőzés', 'El kellett volna utasítani. Kapott: ' + JSON.stringify(r12)); }
    } else { skip('12. Ütköző módosítás megelőzés', 'Nincs elég jövőbeli foglalás az ütközés teszteléséhez (conf=' + conf12.length + ' others=' + others12.length + ')'); }
  } catch(e) { fail('12. Ütköző módosítás megelőzés', e.toString()); }

  // ── 13. Bérlet alkalom levonás ────────────────────
  try {
    var r13all = r(getAllPackages_());
    var pkg13 = (r13all.data && r13all.data.packages ? r13all.data.packages : [])
      .find(function(p) { return p.customerName === testName && p.status === 'Active' && Number(p.sessionsRemaining) > 0; });
    if (pkg13) {
      var before13 = Number(pkg13.sessionsRemaining);
      var rd13 = deductSessionFromPackage(pkg13.packageId);
      var r13all2 = r(getAllPackages_());
      var updated13 = (r13all2.data && r13all2.data.packages ? r13all2.data.packages : [])
        .find(function(p) { return p.packageId === pkg13.packageId; });
      if (updated13 && Number(updated13.sessionsRemaining) === before13 - 1) { pass('13. Bérlet alkalom levonás (' + before13 + ' → ' + updated13.sessionsRemaining + ' maradt)'); }
      else { fail('13. Bérlet alkalom levonás', 'Várt: ' + (before13-1) + ' Kapott: ' + (updated13 ? updated13.sessionsRemaining : 'n/a')); }
    } else { skip('13. Bérlet alkalom levonás', 'Nincs aktív bérlet'); }
  } catch(e) { fail('13. Bérlet alkalom levonás', e.toString()); }

  // ── 14. Szabad időpont ellenőrzés (naptár) ────────
  try {
    var freeDate = new Date(); freeDate.setDate(freeDate.getDate() + 60);
    var avail = isTimeSlotAvailable(formatDate(freeDate), '03:00');
    if (avail === true) { pass('14. Szabad időpont ellenőrzés (60 nap múlva 03:00 → szabad)'); }
    else { fail('14. Szabad időpont ellenőrzés', 'Valószínűleg szabad időpont foglaltként jelent meg'); }
  } catch(e) { fail('14. Szabad időpont ellenőrzés (naptár)', e.toString()); }

  // ── 15. ChangeRequested foglalás megjelenik a listán ─
  try {
    var mb15 = r(getMyBookings_(testEmail));
    var cr15 = (mb15.data && mb15.data.bookings ? mb15.data.bookings : []).filter(function(b) { return b.status === 'ChangeRequested'; });
    if (cr15.length > 0) { pass('15. ChangeRequested foglalás megjelenik az ügyfél listájában'); }
    else { skip('15. ChangeRequested státusz láthatóság', 'Nincs ChangeRequested státuszú foglalás (teszt 11 sikertelen volt?)'); }
  } catch(e) { fail('15. ChangeRequested státusz láthatóság', e.toString()); }

  // ── CLEANUP ───────────────────────────────────────
  var cleanedRows = 0, cleanedCal = 0;
  try {
    var ss = SpreadsheetApp.getActiveSpreadsheet();

    // Bookings sheet
    var bSh = ss.getSheetByName(SHEETS.BOOKINGS);
    var bD = bSh.getDataRange().getValues();
    for (var bi = bD.length - 1; bi >= 1; bi--) {
      if (String(bD[bi][2]).indexOf(TAG) !== -1) { bSh.deleteRow(bi + 1); cleanedRows++; }
    }
    // SessionPackages sheet
    var pSh = ss.getSheetByName(SHEETS.SESSION_PACKAGES);
    var pD = pSh.getDataRange().getValues();
    for (var pi = pD.length - 1; pi >= 1; pi--) {
      if (String(pD[pi][2]).indexOf(TAG) !== -1) { pSh.deleteRow(pi + 1); cleanedRows++; }
    }
    // Customers sheet
    var cSh = ss.getSheetByName(SHEETS.CUSTOMERS);
    var cD = cSh.getDataRange().getValues();
    for (var ci = cD.length - 1; ci >= 1; ci--) {
      if (String(cD[ci][1]).indexOf(TAG) !== -1) { cSh.deleteRow(ci + 1); cleanedRows++; }
    }
    // Financial sheet
    var fSh = ss.getSheetByName(SHEETS.FINANCIAL);
    if (fSh) {
      var fD = fSh.getDataRange().getValues();
      for (var fi = fD.length - 1; fi >= 1; fi--) {
        if (String(fD[fi][4]).indexOf(TAG) !== -1) { fSh.deleteRow(fi + 1); cleanedRows++; }
      }
    }
    // Google Calendar events tagged [TEST]
    try {
      var cal = CalendarApp.getDefaultCalendar();
      var calFrom = new Date(); calFrom.setDate(calFrom.getDate() - 1);
      var calTo = new Date(); calTo.setDate(calTo.getDate() + 100);
      cal.getEvents(calFrom, calTo).forEach(function(ev) {
        if (ev.getTitle().indexOf(TAG) !== -1 || ev.getDescription().indexOf(TAG) !== -1) {
          ev.deleteEvent(); cleanedCal++;
        }
      });
    } catch(calE) { log.push('⚠️  Naptár cleanup hiba: ' + calE); }

    log.push('');
    log.push('🧹 Cleanup: ' + cleanedRows + ' sor + ' + cleanedCal + ' naptáresemény törölve');
  } catch(cleanE) { log.push('⚠️  Cleanup hiba: ' + cleanE); }

  // ── SUMMARY ───────────────────────────────────────
  log.push('');
  log.push('══════════════════════════════════════════');
  log.push('EREDMÉNY:  ✅ ' + passed + ' PASSED  |  ❌ ' + failed + ' FAILED  |  ⏭️  ' + skipped + ' SKIPPED');
  log.push('══════════════════════════════════════════');

  // Re-enable real emails now that tests are done
  PropertiesService.getScriptProperties().setProperty('TEST_MODE', 'false');

  var fullLog = log.join('\n');
  Logger.log(fullLog);
  SpreadsheetApp.getUi().alert(
    failed === 0 ? '✅ Minden teszt átment! (' + passed + '/' + (passed+skipped) + ')' : '⚠️  ' + failed + ' teszt FAILED – lásd részleteket',
    fullLog,
    SpreadsheetApp.getUi().ButtonSet.OK
  );
}

// ============================================
// ONE-TIME CLEANUP — Run manually from Apps Script editor
// Clears ALL rows (except headers) from Bookings, Customers,
// SessionPackages, Financial, PendingBookings sheets.
// Also deletes all Google Calendar events created by the system.
// DO NOT deploy this — run it once from the editor then delete it.
// ============================================
function cleanupAllTestData() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheetsToWipe = [
    SHEETS.BOOKINGS,
    SHEETS.CUSTOMERS,
    SHEETS.SESSION_PACKAGES,
    SHEETS.FINANCIAL,
    'PendingBookings'
  ];

  var report = [];

  sheetsToWipe.forEach(function(name) {
    var sheet = ss.getSheetByName(name);
    if (!sheet) { report.push(name + ': not found, skipped'); return; }
    var lastRow = sheet.getLastRow();
    if (lastRow > 1) {
      sheet.deleteRows(2, lastRow - 1);
      report.push(name + ': deleted ' + (lastRow - 1) + ' rows');
    } else {
      report.push(name + ': already empty');
    }
  });

  // Delete all calendar events that were created by the booking system
  // (looks for events with "Masszázs -" in the title within the last 2 years)
  var deletedEvents = 0;
  try {
    var cal = CalendarApp.getDefaultCalendar();
    var from = new Date();
    from.setFullYear(from.getFullYear() - 2);
    var to = new Date();
    to.setFullYear(to.getFullYear() + 2);
    var events = cal.getEvents(from, to);
    events.forEach(function(ev) {
      var title = ev.getTitle();
      if (title.indexOf('Masszázs -') === 0) {
        ev.deleteEvent();
        deletedEvents++;
      }
    });
    report.push('Calendar: deleted ' + deletedEvents + ' events');
  } catch(e) {
    report.push('Calendar cleanup skipped: ' + e);
  }

  Logger.log('=== CLEANUP COMPLETE ===\n' + report.join('\n'));
  SpreadsheetApp.getUi().alert('Cleanup complete!\n\n' + report.join('\n'));
}

function buildEventDescription(data, sessionNum, totalSessions, packageId, sessionNumber) {
  var desc = 'Kezelés: ' + data.service + '\n' +
             'Név: ' + data.name + '\n' +
             'Telefon: ' + (data.phone || 'Nincs megadva') + '\n' +
             'Email: ' + data.email + '\n' +
             'Megjegyzés: ' + (data.notes || 'Nincs megadva') + '\n';
  
  if (totalSessions > 1) {
    desc += 'Alkalom: ' + sessionNum + '/' + totalSessions + '\n';
  }
  
  if (packageId) {
    desc += 'Bérlet: ' + packageId + '\n';
    desc += 'Bérlet alkalmak: ' + sessionNumber + '\n';
  }
  
  return desc;
}
